/*
 * This file is generated by jOOQ.
 */
package io.github.windyzhu3.ontologylaw.identity.internal.persistence.jooq.tables;


import io.github.windyzhu3.ontologylaw.identity.internal.persistence.jooq.Identity;
import io.github.windyzhu3.ontologylaw.identity.internal.persistence.jooq.Indexes;
import io.github.windyzhu3.ontologylaw.identity.internal.persistence.jooq.Keys;

import java.time.OffsetDateTime;
import java.util.Arrays;
import java.util.Collection;
import java.util.List;
import java.util.UUID;

import org.jooq.Check;
import org.jooq.Condition;
import org.jooq.Field;
import org.jooq.ForeignKey;
import org.jooq.Index;
import org.jooq.Name;
import org.jooq.PlainSQL;
import org.jooq.QueryPart;
import org.jooq.Record;
import org.jooq.SQL;
import org.jooq.Schema;
import org.jooq.Stringly;
import org.jooq.Table;
import org.jooq.TableField;
import org.jooq.TableLike;
import org.jooq.TableOptions;
import org.jooq.UniqueKey;
import org.jooq.impl.DSL;
import org.jooq.impl.Internal;
import org.jooq.impl.SQLDataType;
import org.jooq.impl.TableImpl;


/**
 * Fact
 * Owner：IdentityRuntime；身份主体：一行锚定一个可认证的人或服务身份，由身份域拥有；仅显示名称和生命周期可CAS更新，不保存凭据、Token或任职授权。
 */
@SuppressWarnings({ "all", "unchecked", "rawtypes", "this-escape" })
public class Principal extends TableImpl<Record> {

    private static final long serialVersionUID = 1L;

    /**
     * The reference instance of <code>identity.principal</code>
     */
    public static final Principal PRINCIPAL = new Principal();

    /**
     * The class holding records for this type
     */
    @Override
    public Class<Record> getRecordType() {
        return Record.class;
    }

    /**
     * The column <code>identity.principal.tenant_id</code>.
     * 租户标识：复合主键和所有租户内关联的第一列。
     */
    public final TableField<Record, UUID> TENANT_ID = createField(DSL.name("tenant_id"), SQLDataType.UUID.nullable(false), this, "租户标识：复合主键和所有租户内关联的第一列。");

    /**
     * The column <code>identity.principal.principal_id</code>.
     * 身份主体标识：由应用生成的UUIDv7。
     */
    public final TableField<Record, UUID> PRINCIPAL_ID = createField(DSL.name("principal_id"), SQLDataType.UUID.nullable(false), this, "身份主体标识：由应用生成的UUIDv7。");

    /**
     * The column <code>identity.principal.principal_kind</code>.
     * 身份主体种类：HUMAN或SERVICE，创建后不可修改。
     */
    public final TableField<Record, String> PRINCIPAL_KIND = createField(DSL.name("principal_kind"), SQLDataType.VARCHAR(64).nullable(false), this, "身份主体种类：HUMAN或SERVICE，创建后不可修改。");

    /**
     * The column <code>identity.principal.identity_provider_code</code>.
     * 身份提供方代码：标识静态配置中的认证来源，不保存提供方密钥。
     */
    public final TableField<Record, String> IDENTITY_PROVIDER_CODE = createField(DSL.name("identity_provider_code"), SQLDataType.VARCHAR(64).nullable(false), this, "身份提供方代码：标识静态配置中的认证来源，不保存提供方密钥。");

    /**
     * The column <code>identity.principal.external_subject_hmac</code>.
     * 外部主体HMAC：对提供方主体标识做租户密钥HMAC后的32字节值，不保存外部原文。
     */
    public final TableField<Record, byte[]> EXTERNAL_SUBJECT_HMAC = createField(DSL.name("external_subject_hmac"), SQLDataType.BLOB.nullable(false), this, "外部主体HMAC：对提供方主体标识做租户密钥HMAC后的32字节值，不保存外部原文。");

    /**
     * The column <code>identity.principal.display_name</code>.
     * 显示名称：非权威界面标签，可受控修改，不作为身份匹配依据。
     */
    public final TableField<Record, String> DISPLAY_NAME = createField(DSL.name("display_name"), SQLDataType.CLOB.nullable(false), this, "显示名称：非权威界面标签，可受控修改，不作为身份匹配依据。");

    /**
     * The column <code>identity.principal.state</code>.
     * 身份状态：ACTIVE、SUSPENDED或DISABLED；禁用后不可恢复。
     */
    public final TableField<Record, String> STATE = createField(DSL.name("state"), SQLDataType.VARCHAR(64).nullable(false), this, "身份状态：ACTIVE、SUSPENDED或DISABLED；禁用后不可恢复。");

    /**
     * The column <code>identity.principal.created_at</code>.
     * 创建时间：身份主体首次持久化的时间，创建后不可修改。
     */
    public final TableField<Record, OffsetDateTime> CREATED_AT = createField(DSL.name("created_at"), SQLDataType.TIMESTAMPWITHTIMEZONE(6).nullable(false), this, "创建时间：身份主体首次持久化的时间，创建后不可修改。");

    /**
     * The column <code>identity.principal.disabled_at</code>.
     * 禁用时间：状态首次变为DISABLED时一次写入；未禁用为空。
     */
    public final TableField<Record, OffsetDateTime> DISABLED_AT = createField(DSL.name("disabled_at"), SQLDataType.TIMESTAMPWITHTIMEZONE(6), this, "禁用时间：状态首次变为DISABLED时一次写入；未禁用为空。");

    /**
     * The column <code>identity.principal.revision</code>.
     * CAS修订号：每次受控更新必须精确递增一，初始为零。
     */
    public final TableField<Record, Long> REVISION = createField(DSL.name("revision"), SQLDataType.BIGINT.nullable(false).defaultValue(DSL.field(DSL.raw("0"), SQLDataType.BIGINT)), this, "CAS修订号：每次受控更新必须精确递增一，初始为零。");

    private Principal(Name alias, Table<Record> aliased) {
        this(alias, aliased, (Field<?>[]) null, null);
    }

    private Principal(Name alias, Table<Record> aliased, Field<?>[] parameters, Condition where) {
        super(alias, null, aliased, parameters, DSL.comment("Fact Owner：IdentityRuntime；身份主体：一行锚定一个可认证的人或服务身份，由身份域拥有；仅显示名称和生命周期可CAS更新，不保存凭据、Token或任职授权。"), TableOptions.table(), where);
    }

    /**
     * Create an aliased <code>identity.principal</code> table reference
     */
    public Principal(String alias) {
        this(DSL.name(alias), PRINCIPAL);
    }

    /**
     * Create an aliased <code>identity.principal</code> table reference
     */
    public Principal(Name alias) {
        this(alias, PRINCIPAL);
    }

    /**
     * Create a <code>identity.principal</code> table reference
     */
    public Principal() {
        this(DSL.name("principal"), null);
    }

    @Override
    public Schema getSchema() {
        return aliased() ? null : Identity.IDENTITY;
    }

    @Override
    public List<Index> getIndexes() {
        return Arrays.asList(Indexes.IX_PRINCIPAL__STATE);
    }

    @Override
    public UniqueKey<Record> getPrimaryKey() {
        return Keys.PK_PRINCIPAL;
    }

    @Override
    public List<UniqueKey<Record>> getUniqueKeys() {
        return Arrays.asList(Keys.UQ_PRINCIPAL__PROVIDER_SUBJECT);
    }

    @Override
    public List<ForeignKey<Record, ?>> getReferences() {
        return Arrays.asList(Keys.PRINCIPAL__FK_PRINCIPAL__TENANT);
    }

    @Override
    public List<Check<Record>> getChecks() {
        return Arrays.asList(
            Internal.createCheck(this, DSL.name("ck_principal__disabled_fields"), "(((((state)::text = 'DISABLED'::text) AND (disabled_at IS NOT NULL)) OR (((state)::text <> 'DISABLED'::text) AND (disabled_at IS NULL))))", true),
            Internal.createCheck(this, DSL.name("ck_principal__external_subject_hmac_length"), "((octet_length(external_subject_hmac) = 32))", true),
            Internal.createCheck(this, DSL.name("ck_principal__principal_kind"), "(((principal_kind)::text = ANY ((ARRAY['HUMAN'::character varying, 'SERVICE'::character varying])::text[])))", true),
            Internal.createCheck(this, DSL.name("ck_principal__revision_nonnegative"), "((revision >= 0))", true),
            Internal.createCheck(this, DSL.name("ck_principal__state"), "(((state)::text = ANY ((ARRAY['ACTIVE'::character varying, 'SUSPENDED'::character varying, 'DISABLED'::character varying])::text[])))", true)
        );
    }

    @Override
    public Principal as(String alias) {
        return new Principal(DSL.name(alias), this);
    }

    @Override
    public Principal as(Name alias) {
        return new Principal(alias, this);
    }

    @Override
    public Principal as(Table<?> alias) {
        return new Principal(alias.getQualifiedName(), this);
    }

    /**
     * Rename this table
     */
    @Override
    public Principal rename(String name) {
        return new Principal(DSL.name(name), null);
    }

    /**
     * Rename this table
     */
    @Override
    public Principal rename(Name name) {
        return new Principal(name, null);
    }

    /**
     * Rename this table
     */
    @Override
    public Principal rename(Table<?> name) {
        return new Principal(name.getQualifiedName(), null);
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Principal where(Condition condition) {
        return new Principal(getQualifiedName(), aliased() ? this : null, null, Internal.condition(this, condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Principal where(Collection<? extends Condition> conditions) {
        return where(DSL.and(conditions));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Principal where(Condition... conditions) {
        return where(DSL.and(conditions));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Principal where(Field<Boolean> condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public Principal where(SQL condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public Principal where(@Stringly.SQL String condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public Principal where(@Stringly.SQL String condition, Object... binds) {
        return where(DSL.condition(condition, binds));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public Principal where(@Stringly.SQL String condition, QueryPart... parts) {
        return where(DSL.condition(condition, parts));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Principal whereExists(TableLike<?> select) {
        return where(DSL.exists(select));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Principal whereNotExists(TableLike<?> select) {
        return where(DSL.notExists(select));
    }
}
