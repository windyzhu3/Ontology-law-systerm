# R1 Task9 工作台实施进度

日期：2026-09-08。当前状态：**Task9.1 与 Task9.2a 静态合同已验收及独立复审通过；Task9.2 登录身份后端实施中，尚未整体验收。扩大后的 Task9 与 R1 整体未完成。**

## Task9.2 恢复实施

开始前 52 项架构/角色/合同测试通过。隔离真实 Keycloak 已运行，授权页登录、Code＋PKCE、ID Token 签名/nonce 验证已用于协议夹具；实际观察到旧后端拒绝未逐人注册的新用户，取得有效 RED。动态身份解析已有本地草稿，但尚未通过完整功能验证、独立评审或提交，不能使用。

发现：ADR-0014 取消逐人 HUMAN Registration，而原业务仍允许合法一跳 DELEGATED，并要求回执按原 Actor/on-behalf 精确恢复。新本人任职选择没有代办上下文来源；直接删除旧注册会使生产代办及旧代办回执入口不可达。不能信任 Token claim、按 Task Owner 猜测，或默留旧 HUMAN 注册回退。

用户已确认：补齐基于既有有效 DelegationGrant 的显式代办上下文选择、后端当前复验、actorScopeKey 和原回执恢复规则；不开放 ADM-05 委托授权管理、不新增表、不扩大委托权限，不采用 DIRECT-only 能力降级。

[最小补充设计](../superpowers/specs/2026-09-08-task9-delegated-context-amendment-design.md)已批准并由 ADR-0015 激活。Task9.2a 最终300项全量基线、33项拓扑、19项Java合同、75项前端回归及实际工作树CLI通过；独立评审发现的回执400声明遗漏已修复并复审通过，见[完整静态证据](2026-09-08-task9-delegated-contract-acceptance.md)。这不等于新增代办/登录运行时已通过；现在恢复原Task9.2子代理，不修改实际用户/权限。

### Task9.2 实施检查点（尚非最终验收）

| 后端能力 | 已取得的证据 | 尚待完成 |
|---|---|---|
| 真实登录与动态映射 | `HumanLoginMappingIT` 首批4项通过；修正固定 API audience 与 introspection client 不一致，保留活动性及凭据校验 | 最终生产装配、完整失败边界和回归 |
| 无任职本人会话 | 真实 IdP＋DB＋HTTP 的1项用例由401失败转为通过；返回 NO_APPOINTMENT/no-store，SELF审计落库 | 多任职、失效选择、披露故障和完整边界 |
| 合法代办上下文 | 已取得有效 RED：既有合法关系应返回去重候选1，当前返回0 | 有界候选、显式选择、撤销/DENY、原Actor回执与全部管理接口拒绝 |
| 离线候选与首位管理员引导 | 已冻结合同，尚无本阶段通过证据 | 真账号验证、零写dry-run、原清单重放、部分状态拒绝及原子回滚 |
| 生产装配、角色隔离、独立评审 | 前置52项基线通过，不是新代码验收 | 稳定提交上的完整Task9.2验证与独立评审 |

上述运行日志位于本地 `.superpowers/sdd/2026-09-08-task9-real-user-access-plan/`，包括 `task92-resume-green-01.log`、`task92-self-red-01.log`、`task92-self-green-02.log` 和 `task92-delegated-red-01.log`。局部用例通过不代表浏览器登录、管理建档链或人工UAT通过。

## 当前范围变更与实施差距

用户已将三项要求加入 Task9，并确认采用 Keycloak/OIDC，随后确认[详细设计](../superpowers/specs/2026-09-08-task9-real-user-access-design.md)、[分段计划](../superpowers/plans/2026-09-08-task9-real-user-access-plan.md)和[新增验收矩阵](../acceptance/2026-09-08-task9-real-user-access-acceptance.md)。当前已完成 Task9.1 合同阶段，尚非下列运行时功能的实现。

| 新增要求 | 当前差距 | 实施及验收位置 |
|---|---|---|
| 登录、会话、真实用户进入系统 | 生产入口未接 Keycloak；未完成动态本人映射、任职选择、续期/退出或真实使用者验收 | Task9.1/9.2/9.4/9.6；T9-L、T9-U |
| 受控用户、组织、任职、授权 | 已有底层身份事实与业务鉴权，但没有本次定义的受控建档/管理页面及 HTTP 流程；不能靠 SQL 夹具冒充开户 | Task9.1/9.2/9.3/9.5/9.6；T9-I |
| 完整工作台状态提示 | Task9.0 已有卡、草稿、回执、刷新及等待基础；仍缺新增身份状态、续期保持编辑、重登回执线索及完整状态矩阵实测 | Task9.4/9.5/9.6；T9-W |

详细安全/交互设计已获书面确认；Task9.1 合同及独立评审现已完成，见[本阶段验收证据](2026-09-08-task9-contract-acceptance.md)。下一步为 Task9.2，不提前打开未实现的管理接口。以下旧范围与测试结果保留为历史阶段记录，不充当扩大后 Task9 的完成证明。

## Task9.0 历史范围与用户确认

继续原 Task9：一份响应式 SPA、七类静态责任卡、草稿保存、明确确认提交、回执恢复、当前卡刷新与等待计数。遵循冻结高保真风格，不增加普通工作台导航、侧栏或额外业务页面。

用户确认的后续排期：

| 能力 | 阶段 | 本轮处理 |
|---|---|---|
| 附件上传 | R2 | 不提供上传入口；既有 Evidence 候选只读授权边界保持，不等同于文件上传或下载 |
| 通知 | R2 | 不提供通知铃铛或通知中心；R1 等待数量、窗口聚焦刷新、有界等待刷新保留 |
| 语音 | 后续阶段，尚未指定版本 | 本轮不显示语音入口，不实施录音、转写或 AI |

以上是排期决定，不等于批准 R2 的接口、存储、发送渠道或权限设计。R2 实施前仍须完成相应设计与合同确认。

## Task9.0 历史登录接入边界（已由新增范围取代排期）

此前用户确认没有统一登录系统，先实施不依赖登录选型的部分。此段只记录 Task9.0 当时的实施边界；当前用户已将真实登录纳入 Task9，Keycloak/OIDC 方向已确认。

本轮不新建登录服务，不自行选择身份提供商，不提供粘贴令牌的生产入口，不保存测试身份或绕过后端认证。前端使用可注入的运行时认证边界；未配置身份时安全关闭，显示不可用提示。测试夹具仅用于测试，不能冒充真实用户登录或上线证据。

因此须分别验收：工作台组件和接口调用逻辑、测试环境中的 HTTP 联通、真实身份接入与用户端到端业务闭环。前两者通过也不能宣称第三项已完成。

## 整体进度

| 交付层 | 当前状态 |
|---|---|
| Task1–7：合同、后端业务、责任卡投影、首联/复核/等待恢复与 Worker 核心 | 已有本地交付记录，见 Task8 进度报告 |
| Task8：后端生产认证、HTTP 接线与 API/Worker 装配 | 本地验收完成；本轮不重做 |
| Task9.0：原工作台前端 | 七类卡片、草稿、显式确认、原请求恢复、刷新与等待摘要已有本地验收及独立评审；是历史子阶段 |
| Task9.1：合同后继 | 本地验收及独立复审通过：37/32/5 接口合同、身份/会话/授权边界、NOT_FOUND 一致性；全量基线 296 项通过；不等于新增运行时已实现 |
| Task9.2a：合法代办合同前置 | 最终全量基线300项通过、独立复审通过；静态完成，运行时另验 |
| Task9.2：身份接入后端 | 恢复实施真实登录、动态本人/代办映射、SELF披露和离线引导；已有草稿未验收 |
| Task9.3～9.5：管理、会话与页面 | 尚未实施；管理 HTTP/事务、浏览器登录会话、页面及扩展状态分别验收，新增画面仍需视觉确认 |
| Task9.6：真实登录到七卡及人工用户验收 | 尚未执行；不以原适配器冒烟或首联单卡页面验证替代 |
| 扩大后的 Task9 总体 | 未完成；必须满足新增验收矩阵全部必需项 |
| Task10：联合 E2E、严格 CI、容量门禁 | 尚未完成，本轮不实施 |
| R1 整体 | 未完成，不可宣称完整前后端闭环或可上线 |

## Task9.0 历史证据与仓库状态

Task9 起点：`8ce4d30599cb400ec06b9389a54bf2cefe1edd1e`，分支 `codex/r1-lead-contact-vertical-slice`。

开始实施前，锁定 Node 24.20.0 / npm 11.9.0 下现有前端 7 项测试、类型检查、构建均实际退出 0。这只是原有空工作台/客户端基线，不是 Task9 新功能完成证据。

实现提交：`5136e01af1ba71b3f8fde13e9216c5164ded861a`。评审修复提交：`424f0307c0a9884003bff4de3cc1aa5758b25c64`，这是本次最终验收的代码版本。

初版通过 65 项前端测试；修复新增 10 个回归用例，最终 **75/75 项测试通过（7 个文件）**。控制代理在最终提交上重新运行全量前端测试、类型检查、生产构建，实际退出均为 0，常规检查输出无警告。七类卡片的真实前端请求层冒烟也在修复提交上重跑并退出 0：直接调用新前端 API 适配器，经过真实 HTTP/认证/隔离 PostgreSQL，逐类验证读取 200/304、草稿保存 201 与回读、主命令 200、回执读取及原请求重放一致；联系与调配进入等待时计数为 1。独立 Node 冒烟加载器产生的实验性 TS 转换提示保留在日志，不是常规构建警告。

真实浏览器已走通首联“保存候选 → 整页刷新恢复 → 明确提交 → 当前卡为空、等待 1”；也复验了同卡 200 刷新保留未保存输入。360/768/1440 宽度抽检无横向溢出，控制台 error/warn 为 0；生产构建未配置身份时安全关闭。视觉与范围差异详见根目录 `design-qa.md`。这些证据不是统一登录验收，也不代替现已纳入 Task9.6 的真实身份七类逐屏 E2E 或 Task10 更广门禁。

独立评审第一次结论为 Needs fixes：

1. 连续出现 400/428 修正及后续 stale/digest/主管解析错误时，旧的修正请求键未按最新 NEW_KEY 策略清除。
2. Evidence 最终复验失效时允许产生的 `REJECTED/NOT_FOUND` 终态回执未被前端接受。

两项已通过先失败后通过的回归测试修复：RED 10 失败 / 9 通过，定向 GREEN 19/19，全量 GREEN 75/75。同一独立评审者完成修复差异复核，结论为 **Spec compliant / Approved**，没有剩余 Critical 或 Important 前端问题。未改变样式、后端、合同或生成文件。修复后的真实浏览器再次完成首联保存、整页恢复、提交与等待 1，控制台 error/warn 为 0。

## Task9.0 历史遗留项：终态拒绝枚举（已由Task9.1关闭）

以下保留原阶段的问题记录；该枚举已在Task9.1后继合同中对齐，见[Task9.1验收](2026-09-08-task9-contract-acceptance.md)。它不是当前仍待澄清的合同冲突。

`docs/contracts/r1/R1-HTTP-ERROR-PRECONDITION-MATRIX.md:307` 已允许 Evidence 通过占槽前检查、最终复验失效时形成 `REJECTED/NOT_FOUND` 回执；但 `contracts/openapi/ontology-law-api.yaml:3466` 的 `TerminalRejectionCode` 缺少该值，生成类型也忠实保留了遗漏。这是既存源合同之间的差异，不是简单重新生成即可解决。

本轮仅在前端增加限定为该字面量、保留原生成对象形状的 `PublicReceipt` 运行时兼容，避免合法拒绝被困在恢复中；仍拒绝未知值，不增加权限或披露 Evidence 失效原因。未擅自修改冻结合同。进入后续整体验收前，应单独确认并执行最小 OpenAPI 枚举对齐及正常生成/合同验证；不得将本轮写成“全部合同已一致”。

## 证据索引与环境收尾

本地执行证据目录：`.superpowers/sdd/2026-09-05-r1-business-closure-plan/`。该目录为本地验收材料，不随 Git 提交。

- `task-9-report.md`、`task-9-fix-01-report.md`：实现与 TDD 过程。
- `task-9-review-01.md`、`task-9-review-02.md`：全任务评审及同席修复复核。
- `task-9-final-controller-test.log`、`task-9-final-controller-typecheck.log`、`task-9-final-controller-build.log`：最终提交实际检查，全部退出 0。
- `task-9-final-fix-http-smoke.log`：七类真实前端请求层冒烟，退出 0。
- `task-9-browser-evidence.md`、`task-9-browser-captures/`：真实页面及响应式证据；跟踪报告为根目录 `design-qa.md`。

临时合成身份桥接服务和 QA 开发服务已停止，8819/8820 端口已释放；真实数据库夹具随 Java 正常退出清理。只保留 127.0.0.1:8821 的静态生产构建预览，不带测试身份或认证代理。浏览器临时视口设置已恢复。没有真实用户数据被删除或修改。

此前 GitHub 推送返回 403；本地已正常合入远端 SQL 校验修正，但尚不能声称远端与本地一致。用户随后批准先实施 Task9。本轮不修改认证配置、不强制推送、不部署。

当前步骤：Task9.2 登录身份后端，前置9.1/9.2a静态验收已完成。随后按9.3/9.4→9.5→9.6推进管理、会话、页面与真实用户验收，再进入Task10全BranchID/CI/参考容量门禁。真实登录不再后置；参考容量仍待环境。附件与通知留在R2，语音继续后置。当前只允许隔离合成测试资源，不操作真实人员账号/授权，不提前宣称R1整体完成。
