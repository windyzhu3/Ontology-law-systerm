/*
 * This file is generated by jOOQ.
 */
package io.github.windyzhu3.ontologylaw.party.internal.persistence.jooq.tables;


import io.github.windyzhu3.ontologylaw.party.internal.persistence.jooq.Indexes;
import io.github.windyzhu3.ontologylaw.party.internal.persistence.jooq.Keys;

import java.time.OffsetDateTime;
import java.util.Arrays;
import java.util.Collection;
import java.util.List;
import java.util.UUID;

import org.jooq.Check;
import org.jooq.Condition;
import org.jooq.Field;
import org.jooq.ForeignKey;
import org.jooq.Index;
import org.jooq.Name;
import org.jooq.PlainSQL;
import org.jooq.QueryPart;
import org.jooq.Record;
import org.jooq.SQL;
import org.jooq.Schema;
import org.jooq.Stringly;
import org.jooq.Table;
import org.jooq.TableField;
import org.jooq.TableLike;
import org.jooq.TableOptions;
import org.jooq.UniqueKey;
import org.jooq.impl.DSL;
import org.jooq.impl.Internal;
import org.jooq.impl.SQLDataType;
import org.jooq.impl.TableImpl;


/**
 * Fact
 * Owner：PartyRuntime；主体锚点：一行保存自然人或组织当前规范名、至多一个受保护主标识及一跳合并指向，事实Owner为PartyRuntime；仅允许受控当前态更新，不是案件、客户关系或历史版本表。
 */
@SuppressWarnings({ "all", "unchecked", "rawtypes", "this-escape" })
public class Party extends TableImpl<Record> {

    private static final long serialVersionUID = 1L;

    /**
     * The reference instance of <code>party.party</code>
     */
    public static final Party PARTY_ = new Party();

    /**
     * The class holding records for this type
     */
    @Override
    public Class<Record> getRecordType() {
        return Record.class;
    }

    /**
     * The column <code>party.party.tenant_id</code>. 租户标识：复合主键和所有租户内关联的第一列。
     */
    public final TableField<Record, UUID> TENANT_ID = createField(DSL.name("tenant_id"), SQLDataType.UUID.nullable(false), this, "租户标识：复合主键和所有租户内关联的第一列。");

    /**
     * The column <code>party.party.party_id</code>. 主体锚点标识：由应用生成的UUIDv7。
     */
    public final TableField<Record, UUID> PARTY_ID = createField(DSL.name("party_id"), SQLDataType.UUID.nullable(false), this, "主体锚点标识：由应用生成的UUIDv7。");

    /**
     * The column <code>party.party.party_type</code>.
     * 主体类型：PERSON或ORGANIZATION，创建后不可变。
     */
    public final TableField<Record, String> PARTY_TYPE = createField(DSL.name("party_type"), SQLDataType.VARCHAR(32).nullable(false), this, "主体类型：PERSON或ORGANIZATION，创建后不可变。");

    /**
     * The column <code>party.party.canonical_name</code>.
     * 规范名：当前用于检索和展示的名称，可受控更新；不得混入受保护主标识。
     */
    public final TableField<Record, String> CANONICAL_NAME = createField(DSL.name("canonical_name"), SQLDataType.CLOB.nullable(false), this, "规范名：当前用于检索和展示的名称，可受控更新；不得混入受保护主标识。");

    /**
     * The column <code>party.party.primary_identifier_type</code>.
     * 主标识类型：静态类型代码；空值表示主体没有受保护主标识。
     */
    public final TableField<Record, String> PRIMARY_IDENTIFIER_TYPE = createField(DSL.name("primary_identifier_type"), SQLDataType.VARCHAR(64), this, "主标识类型：静态类型代码；空值表示主体没有受保护主标识。");

    /**
     * The column <code>party.party.primary_identifier_ciphertext</code>.
     * 主标识密文：应用层加密的唯一主标识；空值表示未设置，数据库不可解密。
     */
    public final TableField<Record, byte[]> PRIMARY_IDENTIFIER_CIPHERTEXT = createField(DSL.name("primary_identifier_ciphertext"), SQLDataType.BLOB, this, "主标识密文：应用层加密的唯一主标识；空值表示未设置，数据库不可解密。");

    /**
     * The column <code>party.party.primary_identifier_hmac</code>.
     * 主标识HMAC：用于租户内精确匹配的32字节受保护摘要；空值表示未设置。
     */
    public final TableField<Record, byte[]> PRIMARY_IDENTIFIER_HMAC = createField(DSL.name("primary_identifier_hmac"), SQLDataType.BLOB, this, "主标识HMAC：用于租户内精确匹配的32字节受保护摘要；空值表示未设置。");

    /**
     * The column <code>party.party.status</code>. 主体状态：ACTIVE或MERGED，合并后不得恢复。
     */
    public final TableField<Record, String> STATUS = createField(DSL.name("status"), SQLDataType.VARCHAR(32).nullable(false), this, "主体状态：ACTIVE或MERGED，合并后不得恢复。");

    /**
     * The column <code>party.party.merged_into_party_id</code>.
     * 合并目标主体标识：仅MERGED时存在且直接指向最终活动主体，禁止多跳链。
     */
    public final TableField<Record, UUID> MERGED_INTO_PARTY_ID = createField(DSL.name("merged_into_party_id"), SQLDataType.UUID, this, "合并目标主体标识：仅MERGED时存在且直接指向最终活动主体，禁止多跳链。");

    /**
     * The column <code>party.party.merged_at</code>.
     * 合并时间：仅MERGED时存在，首次写入后不得清空或改写。
     */
    public final TableField<Record, OffsetDateTime> MERGED_AT = createField(DSL.name("merged_at"), SQLDataType.TIMESTAMPWITHTIMEZONE(6), this, "合并时间：仅MERGED时存在，首次写入后不得清空或改写。");

    /**
     * The column <code>party.party.revision</code>. CAS修订号：每次受控更新必须精确递增一，初始为零。
     */
    public final TableField<Record, Long> REVISION = createField(DSL.name("revision"), SQLDataType.BIGINT.nullable(false).defaultValue(DSL.field(DSL.raw("0"), SQLDataType.BIGINT)), this, "CAS修订号：每次受控更新必须精确递增一，初始为零。");

    private Party(Name alias, Table<Record> aliased) {
        this(alias, aliased, (Field<?>[]) null, null);
    }

    private Party(Name alias, Table<Record> aliased, Field<?>[] parameters, Condition where) {
        super(alias, null, aliased, parameters, DSL.comment("Fact Owner：PartyRuntime；主体锚点：一行保存自然人或组织当前规范名、至多一个受保护主标识及一跳合并指向，事实Owner为PartyRuntime；仅允许受控当前态更新，不是案件、客户关系或历史版本表。"), TableOptions.table(), where);
    }

    /**
     * Create an aliased <code>party.party</code> table reference
     */
    public Party(String alias) {
        this(DSL.name(alias), PARTY_);
    }

    /**
     * Create an aliased <code>party.party</code> table reference
     */
    public Party(Name alias) {
        this(alias, PARTY_);
    }

    /**
     * Create a <code>party.party</code> table reference
     */
    public Party() {
        this(DSL.name("party"), null);
    }

    @Override
    public Schema getSchema() {
        return aliased() ? null : io.github.windyzhu3.ontologylaw.party.internal.persistence.jooq.Party.PARTY;
    }

    @Override
    public List<Index> getIndexes() {
        return Arrays.asList(Indexes.IX_PARTY__CANONICAL_NAME, Indexes.IX_PARTY__MERGE_TARGET, Indexes.UX_PARTY__ACTIVE_PRIMARY_IDENTIFIER);
    }

    @Override
    public UniqueKey<Record> getPrimaryKey() {
        return Keys.PK_PARTY;
    }

    @Override
    public List<ForeignKey<Record, ?>> getReferences() {
        return Arrays.asList(Keys.PARTY__FK_PARTY__MERGED_INTO_PARTY);
    }

    @Override
    public List<Check<Record>> getChecks() {
        return Arrays.asList(
            Internal.createCheck(this, DSL.name("ck_party__merge_shape"), "(((((status)::text = 'ACTIVE'::text) AND (merged_into_party_id IS NULL) AND (merged_at IS NULL)) OR (((status)::text = 'MERGED'::text) AND (merged_into_party_id IS NOT NULL) AND (merged_at IS NOT NULL))))", true),
            Internal.createCheck(this, DSL.name("ck_party__not_self_merge"), "(((merged_into_party_id IS NULL) OR (merged_into_party_id <> party_id)))", true),
            Internal.createCheck(this, DSL.name("ck_party__party_type"), "(((party_type)::text = ANY ((ARRAY['PERSON'::character varying, 'ORGANIZATION'::character varying])::text[])))", true),
            Internal.createCheck(this, DSL.name("ck_party__primary_identifier_hmac_length"), "((octet_length(primary_identifier_hmac) = 32))", true),
            Internal.createCheck(this, DSL.name("ck_party__primary_identifier_shape"), "((((primary_identifier_type IS NULL) AND (primary_identifier_ciphertext IS NULL) AND (primary_identifier_hmac IS NULL)) OR ((primary_identifier_type IS NOT NULL) AND (primary_identifier_ciphertext IS NOT NULL) AND (primary_identifier_hmac IS NOT NULL))))", true),
            Internal.createCheck(this, DSL.name("ck_party__revision_nonnegative"), "((revision >= 0))", true),
            Internal.createCheck(this, DSL.name("ck_party__status"), "(((status)::text = ANY ((ARRAY['ACTIVE'::character varying, 'MERGED'::character varying])::text[])))", true)
        );
    }

    @Override
    public Party as(String alias) {
        return new Party(DSL.name(alias), this);
    }

    @Override
    public Party as(Name alias) {
        return new Party(alias, this);
    }

    @Override
    public Party as(Table<?> alias) {
        return new Party(alias.getQualifiedName(), this);
    }

    /**
     * Rename this table
     */
    @Override
    public Party rename(String name) {
        return new Party(DSL.name(name), null);
    }

    /**
     * Rename this table
     */
    @Override
    public Party rename(Name name) {
        return new Party(name, null);
    }

    /**
     * Rename this table
     */
    @Override
    public Party rename(Table<?> name) {
        return new Party(name.getQualifiedName(), null);
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Party where(Condition condition) {
        return new Party(getQualifiedName(), aliased() ? this : null, null, Internal.condition(this, condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Party where(Collection<? extends Condition> conditions) {
        return where(DSL.and(conditions));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Party where(Condition... conditions) {
        return where(DSL.and(conditions));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Party where(Field<Boolean> condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public Party where(SQL condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public Party where(@Stringly.SQL String condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public Party where(@Stringly.SQL String condition, Object... binds) {
        return where(DSL.condition(condition, binds));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public Party where(@Stringly.SQL String condition, QueryPart... parts) {
        return where(DSL.condition(condition, parts));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Party whereExists(TableLike<?> select) {
        return where(DSL.exists(select));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Party whereNotExists(TableLike<?> select) {
        return where(DSL.notExists(select));
    }
}
