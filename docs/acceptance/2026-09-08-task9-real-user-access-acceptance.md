# Task9 扩展验收矩阵

**最新状态（2026-09-12）：** U01～U03人工操作按用户签认关闭，见[记录](task9-real-user-uat-template.md)；执行时间、账号别名和构建／环境信息待补充归档，不冒充代理现场观察。Task9.6q只读等待刷新具名子场景已真实通过：初始读取1次＋自动6次＋手动1次、7次真实304、1次真实续期；同Actor、等待页面与缓存语义保持，暂停提示持续，手动刷新后至少60秒无额外请求。真实进程及独立数据库闭包均退出0；53张非审计表、590条原审计、84张IdP静态表、材料与原检查点未变，仅新增1条准确contact SELF读取审计。只关闭W06的等待态手动／304／语义保持与W08六次额度／续期不重置子集，不关闭非空卡披露审计、整组W06/W08或完整Task9。后文人工延期与q未实施均为历史；完整Task9仍待剩余自动项和最终同构建总评审。详见[最新整链进度](../progress/2026-09-09-task9-local-chain-acceptance.md)。

**最新增量：Task9.6p等待准备已通过（2026-09-12）。** 原六卡记录不重放，新独立5命令／1阶段真实通过，2张DONE＋1张WAITING/revision1、2份CONFIRMED草稿、1份WaitReceipt及5条事件／已投递Outbox核对通过；原54表／537条审计／84张IdP静态表及材料保持，41条新增审计分类。真实进程及最终数据库闭包均退出0。仅关闭T9-W02／03／04中无当前／后续卡、等待数1、准确今日摘要与禁用输入区等具名等待子项；T9-W09只是准备完成，实际Worker到期恢复仍未执行。到期时间`2026-09-14 10:00 +08:00`／原始SLA10:30，不改日期加速。源码`0a1c8ba`及UUID资源校验补丁`5670ba6`均经独立评审；184项受影响回归一次，补丁只跑29项定向及strict，不重放业务。完整Task9仍未完成，人工与容量门不变。详见[本轮闭包与剩余项](../progress/2026-09-09-task9-local-chain-acceptance.md)。

**当前结论（2026-09-12，以下旧状态均为历史）：** 六卡子集已通过前后端真实整链及数据闭包：补齐、路由处置、来源停用请求确认、分配、首联结果、主管复核。原run三阶段／15命令全部确认、无pending，6张卡DONE／6份草稿CONFIRMED，277条新增审计全部分类，原数据保留。最新原第三场景及最终闭包均退出0；工具修正`d77ee27`独立评审Approved，最终157项受影响业务离线仅执行一次，未重复原成功写入。两次线索接入是API准备，不宣称有接入表单；来源请求确认不代表实际停用来源。T9-W01只关闭这六类卡的具名子集，重复确认卡、完整W组／L组／D组／I组剩余场景及U01～03未晋级。Task9.1～9.5源码／分层验收已完成，Task9.6及完整Task9仍未完成。详见[整链进度与下一步](../progress/2026-09-09-task9-local-chain-acceptance.md)。

**2026-09-12 本轮最终状态：** 缓存合同前置修正9.6l已完成源码、63项业务离线／33项身份回归和独立规格／质量评审，最终源码`d77b553`。没有新增真实业务命令或完整验收ID通过。ASSIGN后继版本断言、重启后的原journal衔接、IdP新增用户来源仍需处理；Task9.6／七卡／UAT／R1保持未完成。以下缓存“正在实施”及“未修复”均为旧检查点，当前以本段及[整链最新进度](../progress/2026-09-09-task9-local-chain-acceptance.md)为准。

**2026-09-12 当前增量：** Task9.6l 缓存合同修正正在离线实施，尚未通过评审或真实续验。原九命令／两阶段／无pending及准确既有业务事实保留检查通过；本地服务重启改变 API 进程身份，IdP 的用户／凭据／必办操作／角色映射四表与旧快照不同，正在向用户核实。原release与材料未变，不改旧journal或重放成功命令；真实写入暂缓，自动项未新增通过，U01～U03继续延期。

**当前实际检查点：** 9.6k已记录9／15条命令，补齐／路由／来源请求确认三卡子场景及首联授权测试通过；第二条MANUAL线索已接入，分配草稿尚未派发。真实续验退出1：测试脚本错误地要求工作台`no-store`，而冻结合同为`private, no-cache`与200／304。七卡门及9.6保持未完成；原数据保留核对通过，不以此代替完整业务闭包。下方2026-09-10“前置核对”的全零数据是接入前历史快照，不是当前读数。详见当前自动验收进度。

**最新执行顺序：** 用户要求先搁置人工验收、继续完整推进R1自动验收。U01～U03保持NOT_EXECUTED／延期，不再阻挡独立自动项推进，也不等于豁免或通过。现实施9.6k可构造六卡真实链，重复卡历史候选问题仍单独保留；后续等待／故障／撤权代办、最终同构建及Task10门按原范围推进。阶段状态与实际测试退出见[当前自动验收进度](../progress/2026-09-09-task9-local-chain-acceptance.md)。

**2026-09-10七卡前置核对：** 当前制品运行门和33项测试工具回归通过；本地Party／Lead／Assignment／Task／Draft均0。冻结重复确认候选要求已有RESOLVED Lead关联ACTIVE Party，R1没有Party创建入口，不能以重复capture或直接造Task替代此前置。T9-W01仍未执行，不删减七卡门、不将其余六卡或离线测试等同七卡通过。历史合成数据的最小本地准备方案待单独确认；建议先推进其余六卡，原身份7阶段16命令保持只读，不重建或重放。capture仅作真实API准备，不宣称已有SPA接入表单；来源ACK不代表来源实际停用。详见[当前前置与停点](../progress/2026-09-09-task9-local-chain-acceptance.md)。

**Task9.6j最新结果：** `e6f3a7e`完成已批准的原始引导／后续发布运行核验区分；原Java引导合同与CLI、权限及数据不改。最终定向7／完整本地工具90项通过，Root精确最终源码7项复跑通过，独立复审Approved、无未决finding。真实运行核验5054dc、管理员／未映射协议f9f581、250秒自然刷新8ed937、数据保留97d6e6及最终环境／报告绑定69a117均退出0。刷新HTTP200、新ID Token无nonce后组织读取200并正常加载，原故障关闭；245原审计不变，仅正常读取披露新增4条，84IdP静态表／原材料／三进程／原7阶段16命令保持。应用仍为421ca57／gate12，本轮未重建、重启或重放建档。仅关闭本发布停点和具名自然刷新子场景，不勾选全部L组混合项、七卡、生命周期／恢复或人工UAT，整个9.6仍未完成。详细证据及限制见[本地整链进度](../progress/2026-09-09-task9-local-chain-acceptance.md)。下方历史“须确认／刷新阻断”以本段为准。

**当前门状态（2026-09-10）：** Task9.6i最终`02ce7a0`经两轮修复独立复审Approved；定向32项、Root精确最终代码完整本地部署工具83项退出0。新构建`421ca57`已受控切换至release`6411135a52094b6ba16a80df80b025a1`／gate12；构建退出0、schema不变、五个TLS页面200，新包Worker健康退出0（database／mTLS／三项循环READY、listeners0）。但发布后`bootstrap-verify-current-release`退出失败：该原始核验要求ROOT原名称／revision0，实际合法改名为revision1。原始合同不在本次运行核验修正范围，未放宽或绕过；协议及真实自然刷新尚未执行，整次发布验收、七卡及UAT仍未完成。部署后54表仅gate变化，245原审计／84IdP静态表／原材料及7阶段16命令均保留。下一步须先确认原始核验与后续发布运行核验的最小合同区分。下方为历史增量，以本段最新门状态为准。详见[本地整链进度](../progress/2026-09-09-task9-local-chain-acceptance.md)。

**2026-09-10 Task9.6i执行：** 用户已确认独立Worker运行校验修正；首次授予保持严格，原ROOT合法改名按原Slot／Receipt／Audit版本链核对。首版`9ce2a5a`定向30／完整本地部署工具81项通过；独立评审发现“合法链与无变化／失败尝试并存时误拒绝”一项Important，正在窄修复，尚未部署或关闭运行门。所有真实持续会话、七卡及人工UAT仍保持未完成，见[本地整链进度](../progress/2026-09-09-task9-local-chain-acceptance.md)。

**2026-09-10 Task9.6h增量（源码门完成、未部署）：** 用户已确认下述最小刷新修复；源码`451d0ed`仅修改OIDC adapter及测试，真实锁定SDK刷新路径19项、完整前端462项、离线安全33项及类型检查通过，独立规格／质量评审Approved，无未决finding。首次state／nonce／PKCE保持严格，同会话刷新nonce可缺省、携带须与原值相同，原subject／audience／auth_time绑定不变。发布前既有Worker健康门被ROOT历史改名（名称与revision0→1）和首次授予快照全等校验冲突阻断；尚未构建／停止／部署，旧制品仍运行。修复源码不关闭真实持续会话、七卡或UAT；独立Worker合同修正须另确认，禁止回退原组织或绕过门禁。详见[本轮范围与停点](../progress/2026-09-09-task9-local-chain-acceptance.md)。

**2026-09-10持续会话阻断：** 真实无同步阻塞控制组观察到自然refresh HTTP200、刷新ID Token无nonce后前端退回登录；独立源码检查确认首次nonce必填误用于刷新。登录持续使用／真实轮换相关项仍未通过；测试驱动异步修复的离线结果不能覆盖该产品缺陷。首次state/nonce/PKCE不放宽，最小刷新兼容修复待确认；现场与诊断限制见[最新本地整链记录](../progress/2026-09-09-task9-local-chain-acceptance.md)。

测试驱动`9e4ab39`／`31d34c6`的33项离线、strict类型及独立复审已通过；原环境绑定与已知故障分类反例补齐。该源码门不关闭L组持续会话、任何七卡／生命周期／恢复项或UAT；新独立持续会话入口仍未执行。

**2026-09-10身份链增量：** 9.6e的L01-entry、L03-unmapped、I02-exact-directory-binding、L04-qualification-stages、I05-appointment-no-implicit-grant、I06-minimum-business-grants、I13-dynamic-entry共7项真实子场景已在同run／同制品／同API进程下分段通过；16条真实页面CREATE及Fact／Slot／Receipt／Audit闭包核验通过。详见[最新本地整链记录](../progress/2026-09-09-task9-local-chain-acceptance.md)。这是对应ID的具名子集，不勾选整个混合ID；两次长流程自动化超时仍未关闭，七卡／生命周期／撤权代办等待恢复及用户本人U01～U03仍待执行。不能将重新登录后的分段成功当作持续会话稳定性或Task9总验收通过。

日期：2026-09-08建立，2026-09-09更新。状态：**Task9.1/9.2a静态、Task9.2身份接入、Task9.3受控管理后端、Task9.4会话及Task9.5a/b/c/d源码阶段已验收；9.6真实浏览器整链及人工总验收尚未完成，扩展Task9未完成。用户现已批准仅本地测试环境更新构建／启用Worker／建立专用测试账号和最小权限以推进9.6。**

T9-I02～12的后端／HTTP部分、D05全部管理拒绝及C02～03相关命令／回执回归已取得[Task9.3阶段证据](../progress/2026-09-09-task9-identity-admin-acceptance.md)：最终修复da57aff受影响244项、实际CLI、独立复审通过。四项Important与共享邮箱证据缺口已关闭；这不是混合验收项全部通过，页面和最终管理API建档到真实登录链仍分别由9.5／9.6验收。

Task9.4生产入口`774a301`及正文超时修复`958e190`已完成，L01／L06～12／D08的DOM／transport层证据、修复后最终222项及类型／构建、独立完整评审与复审、受控浏览器和实际缺配置入口抽检见[9.4阶段记录](../progress/2026-09-09-task9-session-acceptance.md)。OpenAPI未改变，装配阶段检查通过。用户明确尚无实际部署配置，本段不是L组／D08混合项总通过，不能替代下述真实Keycloak＋API＋SPA、管理API建档与人工UAT要求。

设计：[真实用户接入与状态设计](../superpowers/specs/2026-09-08-task9-real-user-access-design.md)。计划：[Task9 扩展实施计划](../superpowers/plans/2026-09-08-task9-real-user-access-plan.md)。

视觉门更新（2026-09-09）：用户审阅ADM-01～04四张局部修订图后明确确认[原冻结稿的内容补充](../design/identity-admin-mvp/revisions/2026-09-09-contract-alignment/README.md)。原布局／样式仍有效，仅字段、控件、分页、层级说明和合同对应操作文案获批；此前替代布局作废。此项是视觉输入确认，不新增功能完成证据，不关闭任何页面、交互或真实整链混合验收项。

Task9.5a非视觉管理适配器、9.5b四页读取／准入、9.5c十四写入和9.5d完整工作台状态均已通过各自源码／受控UI门及独立评审。最新实现`366468b`／修复`da9d289`；首版443项全量、修复后93项受影响回归，Root实际浏览器复验及基线／拓扑通过。详见[管理前端阶段进度](../progress/2026-09-09-task9-identity-frontend-integration.md)。这些是对应C/L/I/W组的前端层证据，仍不关闭必须真实管理建档→登录→责任卡链的混合项或人工UAT；本次9.6起点另有445项完整前端回归通过，不能代替整链。

阶段证据：[Task9.1 本地验收](../progress/2026-09-08-task9-contract-acceptance.md)。C01 合同及 C02～04 静态部分已验证；C02～04 的运行时权限、错误配置、回执披露仍随后续实现验收，不将这些混合项整体提前判为通过。

代办接入补充：[具体补充设计](../superpowers/specs/2026-09-08-task9-delegated-context-amendment-design.md)已获用户书面确认，T9-D01～08正式加入本矩阵；T9-D01静态项已通过，见[代办合同验收](../progress/2026-09-08-task9-delegated-contract-acceptance.md)。D02～07的9.2后端实现已有[运行时阶段证据](../progress/2026-09-09-task9-identity-runtime-acceptance.md)，D05全部20管理HTTP处理现有9.3阶段证据；D03浏览器、D08会话恢复及最终同构建总验收仍待后续，不替代原C/L/I/W/U验收。

### Task9.2 阶段证据定位（不是本矩阵总验收）

| 验收组 | 本阶段证据 | 尚未满足的总验收部分 |
|---|---|---|
| L02～05、I13 | 真实OIDC／当前DB映射／本人及代办选择与故障分类，最终源码581c61e | SPA交互、通过管理API建目标用户及真实使用者链 |
| I01、C04 | 离线候选／引导／原结果核验、HTTPS与独立身份库TLS、错误配置拒绝 | 实际部署运维；真实人员执行另须授权 |
| D02～07后端 | 有界当前候选、真实动态业务Actor与回执、审计／锁／撤权／范围验证 | D03浏览器链、D05管理HTTP完整处理；D07单序列E2E补强 |
| C02～03相关回归 | 原架构、角色、SERVICE/mTLS、业务与回执回归及实际CLI | 新Identity管理命令／Receipt与最终完整SPA/E2E仍随9.3～9.6 |

自动证据为隔离真实Keycloak＋PostgreSQL＋HTTP及生产装配，不是模拟Token；部分状态由窄用途SQL测试夹具准备，不能替代下节规定的最终管理API建档链。所有必需ID仍须在9.6最终构建重验；非阻断测试限制详见阶段记录。

## 1. 通过条件

必须在同一最终构建上通过下面全部必需项，完成独立评审，并附实际执行用例、退出码、脱敏截图/日志和人工 UAT 记录。测试跳过、mock 身份、只有 API 请求层证据或只展示冻结图片均不能通过。原 75 项测试属于回归基线，不是新增验收的完成证据。

自动化环境必须运行真实 Keycloak、应用 PostgreSQL、API 和 SPA；等待恢复场景加入实际 Worker。目标账号通过 Keycloak 受控创建，目标 Principal/组织/任职/Grant 通过管理 API/页面创建，不用 SQL 插入这些目标身份事实。只允许一次性 bootstrap 初始化信任根。业务数据采用隔离合成资料，沿实际 capture/分配/后继命令产生责任，不直接把 Task 插成所需终态。

人工 UAT 必须由指定真实使用者自行输入凭据；自动化测试使用的可登录合成账号不能冒充人工 UAT。不得记录密码、完整 Token、私钥、原始 subject、HMAC、未脱敏个人资料。

## 2. 合同与架构前置

| ID | 场景与步骤 | 必须结果 |
|---|---|---|
| T9-C01 | 检查具名后继 ADR、基线、四份功能合同、OpenAPI 与生成文件 | 不再存在 R1 明确排除 ADM-01～04/登录却实施它们的冲突；37/32/5 提案按最终批准值精确冻结，不能硬保留旧 16/11/5 |
| T9-C02 | 执行 OpenAPI mutation/生成漂移、拓扑、权限能力及架构检查 | 拒绝多 SPA、新应用表、业务 Tenant 参数、匿名管理接口、Worker 取得用户/管理能力；旧迁移字节保持 |
| T9-C03 | 验证终态 NOT_FOUND 与新增 Identity Receipt | HTTP/OpenAPI/runtime/前端一致，Identity 回执仍限原 Actor 当前权限；原 R1 九公共命令 shape 和十四业务事件保持 |
| T9-C04 | 未配置/错误 issuer、JWKS、introspection secret、realm/Tenant 绑定及 TLS | readiness/请求失败关闭，无测试身份或逐人硬编码回退；身份基础设施及独立数据库拓扑明确记账 |

## 3. 登录与会话

| ID | 场景与步骤 | 必须结果 |
|---|---|---|
| T9-L01 | 新浏览器从 /workbench → /login → Keycloak 输入有效凭据 → 回跳 | 校验 code/state/nonce/PKCE 后进入真实本人 context；URL 和浏览器持久存储没有 access/refresh token |
| T9-L02 | 错误密码、禁用 IdP 账号、非法/重放回跳、非 allowlist 回跳地址 | 不建立业务会话，不显示业务卡；提示安全且可重新登录，不泄露账号存在性 |
| T9-L03 | IdP 账号存在，但没有 Principal 映射；映射冲突或 provider 错误 | 拒绝业务接入，不自动开户、不按姓名合并、不自动授予任职/权限 |
| T9-L04 | 本人已映射，分别没有有效任职、只有一个任职、存在两个任职 | 无任职明确提示；单个可自动选；多个要求显式选择，不猜第一个 |
| T9-L05 | 篡改 X-Appointment-Id 为他人/其他 Tenant/过期/挂起任职 | 后端拒绝，不以浏览器选项/IdP role 作为授权；实际用户与 Owner 不混淆 |
| T9-L06 | 有权限用户整页刷新并经已有 SSO 会话回跳 | 不使用旧内存 Actor；重新验证本人、任职、权限，恢复服务端 Draft 与正确 envelope |
| T9-L07 | 输入尚未保存内容/保持未决写请求，触发同身份 token rotation | identity epoch 不变，dirty 文本、原 commandId/key/body、轮询额度保留；不重复写入 |
| T9-L08 | 刷新 token 失败、idle/absolute 到期、IdP 故障、introspection 超时 | 区分确定失效与暂不可用，停写、安全重登；不缓存成功校验后绕过 IdP，不无限重试 |
| T9-L09 | 主动退出、其他标签页退出、浏览器后退、重新聚焦旧标签页 | 清除敏感页面与缓存；后续旧 token 经活动性复核被拒绝；IdP 退出失败不得显示全局退出成功 |
| T9-L10 | 只改变用户/任职/realm，再让旧 GET 和旧 POST 响应返回 | 旧 scope 的迟到结果不能显示、不能驱动新 scope 计数或业务提交 |
| T9-L11 | 同页断网丢失 POST 响应；随后 session 失效、重登，使用最小恢复标记 | 只在同 actorScopeKey 查询原回执；终态只处理一次；未保留完整正文时没有原请求重试按钮 |
| T9-L12 | 篡改/跨身份/过期恢复标记、sessionStorage 禁用、legacy metadata 不可用；未决时切换管理页面并尝试新写请求 | 不越权，不把失败 GET 当命令失败，不重新生成业务请求；标记无法持久化时不得派发写请求，未决标记不能被新写请求覆盖；同 Actor 重登/重启后 scope key 稳定，换 Actor 不同 |

### 3.1 合法代办接入与原身份恢复（新增必需项）

| ID | 场景与步骤 | 必须结果 / 负责阶段 |
|---|---|---|
| T9-D01 | header/两字段/闭合 DTO/state 变异，实际生效后继解析，原业务 body/权限/事件/物理对比 | 不接受任意 Principal/Grant/路径输入；37/32/5、原九业务 body/策略/事件/物理字节保持；9.2a 静态合同门 |
| T9-D02 | 无代办选择、合法单条关系、多条同双方关系、50/51 候选；外部/失效/伪造选择 | 无隐式代办；候选去重、有界；无越权披露或静默回退；9.2 |
| T9-D03 | 实际 OIDC 动态登录，显式本人/合法代办任职，读取/保存/完成责任 | 无逐人注册；Owner 未变；实际办理人与被代办人审计准确；动作分别复验准确权限/scope/DENY；9.2 API +9.6 浏览器 |
| T9-D04 | 双方身份/任职/组织/委托/source Grant 挂起、撤销、过期，对象 DENY 和双连接撤销竞争 | 初始/最终检查及披露拒绝对应失效；保持原持锁裁定点；9.2 |
| T9-D05 | 代办上下文请求全部20管理 operation；SERVICE/mTLS 携带选择器 | 拒绝，不降级为本人、不转授管理、不改绑 SERVICE，零业务写；9.2/9.3 |
| T9-D06 | 代办候选 SELF 审计成功、失败、未知提交、响应上限、未授权探测 | 先审计后披露；SELF 主体是登录本人；max101准确引用；拒绝不透露目标存在性；9.2 |
| T9-D07 | 原代办 Actor 写入回执后重登/重启/改选同双方合法授权证据，再改变任一 Actor 分量 | 同元组 scope key 稳定、原回执可达；不同元组拒绝；不绕过当前授权或修补历史回执；9.2 |
| T9-D08 | 未决代办标记重登后先进入本人 context，再显式选择原代办；确认换身份、迟到响应、Token 轮转 | 初始选择未完成不丢线索/不新写/不遍历身份查回执；同 scope 恢复；切换清旧视图并确认放弃风险；9.4/9.6 |

代办兼容测试只允许隔离环境预置既有 DelegationGrant 关系；目标用户/组织/任职/直接业务 Grant 仍经真实受控管理链建立。这是验证旧关系接入的测试例外，不是 ADM-05 产品能力，也不准通过新增生产委托写入口方便验收。具体边界、错误、安全存储及审计形状以批准补充设计及激活后的合同为准。

## 4. 受控身份管理

| ID | 场景与步骤 | 必须结果 |
|---|---|---|
| T9-I01 | 空的受信部署按清单执行一次性 bootstrap，再重复/改清单/从 HTTP 尝试 | 仅初次准确建立 Tenant/根组织/首位管理员/四项管理 Grant；同引导可核验，变更清单或网页入口不能追加管理员 |
| T9-I02 | 身份运维在 Keycloak 创建账号，ADM-01 以完整用户名精确搜索并绑定准确账号 | 用户2026-09-09确认候选0～1项、nextCursor=null，不按姓名／邮箱／模糊搜索；验证不存在／停用／SERVICE无候选、查询不触发通配／特殊lookup、IdP故障安全失败。业务库仅保存provider、subject HMAC、显示名等冻结字段；外部账号与本地身份分别成功，不伪装跨库原子事务 |
| T9-I03 | ADM-01 重复同账号绑定、同名不同 subject、跨 realm 候选、绑定提交丢回包 | 不重复建身份、不按名称合并、不跨 Tenant；只使用原 key 恢复 |
| T9-I04 | ADM-02 建根下组织、改名、试图改 code/重挂父级/关闭有有效子组织或任职的节点 | 允许项准确 CAS；不允许项安全拒绝，无部分写入；关闭不可逆 |
| T9-I05 | ADM-03 为用户创建准确岗位任期，修改冻结字段或重复结束 | 岗位不自动授予权限；冻结 principal/org/role/任期不改；ENDED 不恢复 |
| T9-I06 | ADM-04 授予 R1 静态权限、范围、有效期；尝试扩大 scope、自授权、转授管理/SYSTEM 权限；局部组织管理员尝试读取/挂起跨组织 Principal 或枚举无任职账号 | 仅服务端 allowlist、准确任职、管理 scope 内允许；对象 ALLOW/IdP role 不能替代直接管理 Grant；Tenant 级 Principal/IdP 候选及新任职用户候选严格要求设计规定的根 scope |
| T9-I07 | 撤销 Grant 后持旧 token、ETag、Draft 重读/提交/查原回执 | 当前权限重验生效，200/304 不泄露撤权内容；原请求恢复不绕过当前授权 |
| T9-I08 | 挂起 Principal 或 Appointment；随后恢复；结束仍拥有 OPEN/WAITING Task 的任职 | 安全挂起立即阻止访问；恢复遵循状态机；结束被依赖检查阻止，不能暗中转派或完成 Task |
| T9-I09 | 普通用户直接访问 /admin/identity/* 并手工发管理 HTTP 请求 | 页面与 API 双重拒绝，不仅隐藏按钮；管理身份不会因此获得销售业务卡 |
| T9-I10 | 尝试禁用当前管理员、结束其当前管理任职、撤掉最后可用引导管理资格 | 拒绝在线自锁与越权提权，不提供默认超级管理员后门 |
| T9-I11 | 两连接竞争 CAS、撤权与业务最终复验、组织关闭与新建任职 | 锁序无反向死锁，旧版本不得覆盖；不同 Tenant 可独立推进；裁定点符合 ADR-0006 |
| T9-I12 | Identity Fact/Audit/Slot/Receipt 任一写入失败、提交确认丢失、重放和冲突 | 正常错误原子回滚；未知提交保留原 key；重放不重复 Fact，冲突无新增业务 delta |
| T9-I13 | 完成用户建档后，不改逐人部署注册、不重启 API，让该用户登录 | 动态可信映射生效，正确取得任职资格；SERVICE/mTLS 既有精确注册仍受原限制 |

T9-I01 追加必需回归（2026-09-09用户批准）：引导完成后新增身份／组织／任职／业务授权／委托／对象规则和其他命令记录，仍用原 manifest／command／密钥核验原准确事实与原闭包，返回原回执、delta0；新增记录不因此被认证合法。原集合缺失、字段／版本／生命周期变化、原 Grant ID与权限码对应被改或闭包损坏时仍拒绝且不修补。覆盖准确候选到期及IdP不可用下完整／损坏原结果两种分支；纯 verifier SQL 夹具不替代真实受控开户E2E。该补充由9.6a修复门承接，总验收仍须同最终构建重验。

9.6a阶段证据已通过：生产修复e925380、测试补强9a7ea56，99项受影响回归及补强后35项通过、独立复审关闭；保留的本地原清单／密钥两次离线核验退出0，54张表和原文件内容摘要不变。详见[阶段记录](../progress/2026-09-09-task9-bootstrap-original-set-verification.md)。此项不激活新API制品，也不将完整I01／9.6／人工UAT提前判为通过。

## 5. 工作台状态与前后端闭环

| ID | 场景与步骤 | 必须结果 |
|---|---|---|
| T9-W01 | 通过管理与实际 R1 业务链准备七类责任，分别以其 Owner 登录 | 每类卡均经页面保存候选、明确主提交、取得准确 Receipt/Fact 和后继状态；不能仅用七个适配器测试替代 |
| T9-W02 | 初次读取慢响应、失败、有效零态、仅等待、无入口资格 | 加载/未知/无任职/无权/零态/仅等待文案区分；未知计数不显示为 0 |
| T9-W03 | 服务端含当前卡、0/1/2 后续摘要、等待 0/正数、跨业务日刷新 | 一句今日摘要准确；最多一张完整卡和两条摘要；当前卡数量不冒充今日总量；时间按业务时区 |
| T9-W04 | 输入不完整、保存中、保存成功、已保存后修改字段 | 保存和业务确认明确分离；未保存改动时主提交禁用，条件字段及证据只读边界不退化 |
| T9-W05 | 双击主提交、回包丢失、明确拒绝、已成功但刷新失败 | 至多一次业务结果；显示“已记录但刷新失败”而非重提建议；未知结果不当失败 |
| T9-W06 | 手动刷新、focus 刷新、304、旧 GET 晚到、同卡语义不变 200 | 保留正确 envelope、未保存输入和逻辑焦点；304 已完成后端审计；旧响应不能倒退 |
| T9-W07 | Task/Draft/selector 改变、412、400/428→stale/digest/422 | 安全重载提示与最新 retry policy 正确；不保留过时修正键，也不在未知结果时换键 |
| T9-W08 | 切后台、卸载、自动等待刷新 6 次/回执查询 3 次额度耗尽、Token 续期 | 清理 listener/timer；有界轮询，续期不重置额度；额度用尽提示手动刷新 |
| T9-W09 | 实际 Worker 将 WAITING 恢复为 OPEN，用户刷新 | 原责任按合同恢复，新 revision 可见，等待数来自新 envelope；不把 Worker ack 当成客户端刷新 |
| T9-W10 | 网络离线、429、503、401/403/404、Evidence 撤回 | 区分可重试故障与权限失效，旧内容标陈旧停写或清除；不展示不可见对象存在性 |
| T9-W11 | 360/768/1440、键盘、弹层、会话操作、错误提示、reduced motion | 页面遵循冻结风格；固定 composer 不遮挡输入/提示；焦点可见，状态不只靠颜色，live region 不刷屏 |
| T9-W12 | 相同 Token 下组织/权限变化、另一个 Tenant 同名账号、伪造 provider role | 当前 DB 权威与 DENY 优先；本人/Owner/管理员三者身份不混淆，跨 scope 零泄露 |

## 6. 人工真实用户验收

| ID | 场景与步骤 | 必须结果 |
|---|---|---|
| T9-U01 | 指定管理员按运维＋ADM-01～04 流程为真实试用者建立账号和资格 | 记录脱敏账号别名、构建、操作时间和结果；无 SQL/注入 JWT/测试登录替代 |
| T9-U02 | 试用者自己输入凭据登录，处理一张有权责任卡，保存、提交、刷新/重进 | 看见准确本人/任职、正确责任及反馈；Receipt 和当前数据库事实对应 |
| T9-U03 | 管理员撤权或挂起，试用者刷新；随后受控恢复资格并重新登录，最终退出 | 撤权真实生效，恢复不复活旧 Grant/终态任职，退出后不再看见原卡 |

## 7. 证据格式与完成判定

每个 ID 对应一条机器或人工记录：`caseId`、`buildSha`、`environmentDigest`、`executedAt`、`status`、`reportPath`，自动用例另有测试工具原始 case identity 与实际退出码，人工用例另有使用者别名和确认结果。环境摘要含应用制品、Keycloak/数据库镜像 digest、配置脱敏摘要、浏览器及工具链版本，不含秘密。

通过条件是全部必需 ID 有真实成功证据、无 skip/零测试/假占位、独立评审无阻断问题；修改认证/授权/状态代码后须重跑受影响项并绑定新构建。扩大后的 Task9 完成不等于 Task10、容量或整个 R1 上线完成。
