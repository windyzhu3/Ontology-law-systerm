# Task9 责任工作台视觉与交互验收

> 范围更新：本报告仅作为 Task9.0 历史视觉证据。2026-09-08 新增登录/任职选择、受控管理及状态要求见[扩展设计](docs/superpowers/specs/2026-09-08-task9-real-user-access-design.md)；这些新增画面尚未实施或视觉验收，不继承下面的通过结论。

日期：2026-09-08。视觉检查代码：`5136e01`；最终修复代码：`424f030`。两者样式及页面组件无差异，已用 Git 差异检查确认；修复后再次完成真实页面流程。本报告只覆盖批准的登录独立 Task9，不代表真实登录、Task10 或 R1 发布验收。

## 视觉依据与证据

- 视觉依据：`docs/design/sales-mvp-workcards/frozen/01-contact-lead-review.png`；其余五张冻结卡片及 README 作为共用样式依据。
- 当前 Workbench / HTTP 合同优先于图片里的示例业务。通知、附件、语音入口按用户决定删除；没有可信当前用户资料时不显示模拟用户头像；图片中的城市、来访等示例不转化为未批准的字段。
- 真实组件在隔离 QA 页面渲染；`visual=5` 为首联候选已保存、两条后续摘要、等待 1 的合成展示状态，不是生产 mock 路由。真实 HTTP 场景另行验证。
- 截图目录：`.superpowers/sdd/2026-09-05-r1-business-closure-plan/task-9-browser-captures/`。截图是浏览器原始 JPEG 字节，未经图像编辑。

| 证据 | CSS 视口 | 实际截图像素 | 状态 |
|---|---|---|---|
| `desktop-contact-final.jpg` | 1485×1059 | 1470×1048 | 已保存首联候选，与 BASE01 同轮联合输入比较 |
| `desktop-1440-final.jpg` | 1440×1059 | 1425×1048 | 同一首联展示状态 |
| `tablet-768-final.jpg` | 768×1024 | 753×1004 | 滚动末尾，后续摘要和等待计数完整可访问 |
| `mobile-360-focused-final.jpg` | 360×800 | 345×767 | 联系说明聚焦、未保存候选、主提交禁用 |
| `real-contact-completed-final.jpg` | 1485×1059 | 1485×1059 | 真实 HTTP 提交后空当前卡、等待 1，带合成验收工具标识 |

BASE01 为 1485×1059；浏览器 DPR 约 1。带滚动条的原生截图输出省略滚动条并有轻微尺寸归一化，因此以 CSS 实测尺寸校验布局，不把像素差当成设计问题，不宣称逐像素一致。没有拉伸、拼接或重绘图片。

完整比较：在同一次浏览器工具输入中同时展示 BASE01 原图与修复后的首联截图，核对整体框架、卡片、候选区、主按钮、后续等待区和底部输入区。全幅图片中的这些区域及文字可清楚辨认，故没有另作可能改变密度的局部裁剪。手机版聚焦截图用于单独检查实际编辑区域和底部输入区的关系。

## 五项视觉核对

| 表面 | 结果与限定 |
|---|---|
| 字体与层级 | 中文系统无衬线栈，品牌 23px、桌面主标题最高 34px、候选标题 21px、表单 16px，移动标题 26px；主次层级与冻结风格一致。辅助文案比旧示例紧凑，属于 P3 精细排版余量；不能从栅格原图确定唯一字体文件，不擅自引入新字体。 |
| 间距与布局 | 74px 页头、居中 1144px 桌面卡片、两栏分隔、窄屏单栏、6px 圆角、1px 边界、弱阴影。合同要求的实际表单和业务目的增加卡片高度，接受这一内容差异。最终桌面等待区底 901.09px，输入区顶 907.88px，未重叠。平板正常滚动后等待区底 774.09px、输入区顶 872.54px。 |
| 颜色与 tokens | 冻结暖白 #f8f7f7、石墨 #2c2c34、祖母绿 #007d52、薄荷 #f1f6f4、边界 #dfe0e4 已对应。白字/绿按钮对比度 5.18:1，辅助字 #58616c/白底 6.28:1，正文/暖白 12.95:1。未进行完整 WCAG 认证。 |
| 资产与清晰度 | 使用 Phosphor 图标库，不使用手工 SVG、emoji 或图片冒充页面；该工作台不需要照片或插画。图标清晰，语义与冻结图相符，用户批准后置的入口不伪装成可用控件。 |
| 文案与内容 | 使用当前服务端授权摘要、负责人、选项及静态表单，不冒充当前登录身份，不泄露选择器 UUID/hash/raw code。草稿保存与唯一业务确认分开，输入区明确保存到哪个字段，不暗示 AI、语音或上传。 |

## 发现与修复历史

1. **P2：桌面输入区遮挡等待区。** 初次同视口比较中等待区底 895.16px，输入区顶 889.88px，重叠 5.28px。将候选输入高度收敛到 44px；同状态重新联合比较后等待区与输入区有 6.78px 正间距，修复成立。桌面间距仍比原示例紧凑，可作为 P3 精细调整，但不增加本轮样式重设计。
2. **P1：同卡 200 刷新覆盖尚未保存的输入。** 浏览器观察到服务端返回语义相同的新对象时本地编辑丢失。修复为按 Task/Draft/selector/form 编辑上下文区分刷新；相同上下文保留 dirty，不同上下文安全重载并提示。回归测试先失败后通过；最终桌面和 360 手机手工编辑后点击刷新，卡片字段和底部候选均保留，主确认保持禁用。
3. **不作为缺陷：** 实现过程中的 Vite 热重载曾使验收工具重新挂载，已用冻结代码完整重做；平板首屏的等待区需要继续滚动，末尾可完整访问，并非永久遮挡；生产无身份显示不可用是用户批准的安全边界。

## 实际交互与剩余边界

- 最终真实页面：选择电话/未能联系 → 输入候选 → 保存 → 整页刷新恢复相同 Draft → 明确点击记录联系结果 → 当前卡为空且等待 1。
- 七类真实前端适配器流程分别经过 HTTP、真实认证解析和隔离 PostgreSQL；此证据不等同于七类逐屏浏览器 E2E。
- 360/768/1440 无横向溢出；手机聚焦字段底 447.80px，底部输入区顶 592.75px，可见且有焦点轮廓。标签、必填、live 状态、减弱动画与键盘逻辑由 DOM 测试及本次浏览器抽检覆盖。
- 浏览器本轮最终读取 error/warn 日志为 `[]`。生产构建 `/workbench` 无 session 显示登录未接入，不提供业务卡片或测试身份。
- 两项恢复边界已修复，同一独立评审复核通过；最终 75 项前端测试、类型检查、构建及七类真实请求层冒烟通过。修复后页面完成证据为同目录 `real-contact-fixed-final.jpg`。本文件的 passed 仅为视觉与已列页面交互，不替代真实登录或 Task10 门禁。
- 临时带合成身份的 QA 服务已停止；只保留无身份的静态生产预览 127.0.0.1:8821/workbench，临时浏览器视口已恢复。

## 验收清单

- [x] 打开实际冻结视觉与渲染页面并联合比较。
- [x] 修复遮挡及输入丢失，补回归证据。
- [x] 检查五项视觉表面、三个响应式宽度、真实首联流程与控制台。
- [x] 列出批准的内容差异、密度差异和未覆盖的完整 E2E/登录边界。

final result: passed

## Task9.4 LOGIN-01增量QA — 2026-09-09

本节是`73aeef4`登录组件的独立增量，以上Task9.0证据保留为历史。使用image-to-code/design-qa流程逐项对照用户选择的第3稿；既有冻结风格优先于通用UI建议，未另加品牌、字体或导航。本结果不代表生产登录入口、完整9.4或真实用户验收通过。

视觉源：`docs/design/session-access/frozen/LOGIN-01-brand-entry.png`，原图1487×1058，SHA-256`3d0ab2d5ccdf9e26e556a3e13dd5f9fb7d7a45ecb93d5797a00c94a6d6009db2`。本地证据目录：`.superpowers/sdd/2026-09-08-task9-real-user-access-plan/task94-login-captures/`。使用忽略的Vite预览加载真实LoginPage组件，不连接IdP、不修改生产main/config。

| 证据文件 | CSS视口 | 实际截图像素 | 检查 |
|---|---|---|---|
| desktop-04.jpg | 1487×1058 | 1487×1058 | 与冻结原图同轮联合展示比较 |
| desktop-pending.jpg | 1487×1058 | 1487×1058 | Tab焦点、Enter激活后禁用及跳转提示 |
| desktop-1440.jpg | 1440×900 | 1440×900 | 常用桌面尺寸、无溢出 |
| tablet-768.jpg | 768×1024 | 768×1024 | 平板两栏、文案换行、主操作完整 |
| mobile-360.jpg | 360×800 | 360×800 | 单栏、按钮304×61.33CSS像素、提示可读 |
| mobile-long-name.jpg | 360×800 | 345×767 | 忽略夹具替换成长名称，正常换行、无横向溢出、页脚可滚动访问 |

以上实际文件尺寸用System.Drawing读取验证。长名称出现垂直滚动条时截图有尺寸归一化，DOM内宽360、内容宽345、scrollHeight819；End滚动后页脚底771.88在800高视口内。原图未缩放、裁切、重绘；全幅关键文字与界面均清晰可辨，无需另作可能改变密度的局部裁剪。

五项核对：①字体沿用中文系统栈，桌面主标题52px、辅助26px，层级与源一致；栅格原稿与本机字体细微字宽差异记为P3余量，不擅自换字体。②44/56分栏、108px Logo、桌面74px按钮及留白关系一致，窄屏按原布局顺序堆叠。③沿用暖白、薄荷、石墨和翡翠绿tokens，细边界和6px圆角，未增加装饰体系。④保留用户明确要求的Logo／律所名称占位，用既有Phosphor箭头，无新品牌图片。⑤静态中文与冻结稿一致，只有一个登录动作，无密码／Token／注册等越界入口。

交互：Tab到唯一按钮，焦点轮廓可见；Enter后同步禁用并显示礼貌live状态，预览3秒后解除禁用并明确“不连接统一身份服务”。真正的controller/provider登录调用、缺配置拒绝、安全失败重试和卸载后的迟到失败由8个新增组件／集成测试覆盖，不将预览延时当作OIDC证据。360/768/1440均无横向溢出。

诊断记录：初始忽略夹具采用预构建文件路径导入造成React导出／重复实例错误，改为标准bare imports并在Vite优化完成后重载解决，无生产配置或依赖修正。另有一次键盘工具参数类型错误，改用已支持的locator.press后完成检查。旧控制台记录保留；最终读取2026-09-09T03:01:00Z之后error/warn为`[]`。初始desktop-01原生截图裁切，desktop-02/03的fullPage/clip捕获改变了内容密度，不能据此修改CSS；最终用默认viewport截图desktop-04重新与原图联合比较，未发现阻塞视觉问题。

剩余门禁：任职／合法代办及完整恢复状态视觉先确认，再完整装配main/config和会话路由；全Task9.4独立评审、真实Keycloak/API/SPA登录领卡及9.6/UAT仍未完成。当前服务仅127.0.0.1:4174本地视觉预览，浏览器临时视口已恢复，未部署或推送。

LOGIN-01 visual/interaction result: passed (bounded component preview only)

源码评审补充：首评发现初始化前短暂可激活的问题，已在`4ebc485`补provider就绪门禁和首次／替换commit回归，修复定向26项及typecheck通过。LoginPage和CSS未改，上述视觉证据仍对应当前页面字节；该门禁修复不以纯视觉预览代替真实生命周期测试，独立复审另见阶段记录。

## Task9.4 CHOICE-01增量QA — 2026-09-09

本节仅验收用户本轮选择的第1稿：本人任职／合法代办选择。原图`docs/design/session-access/frozen/CHOICE-01-appointment-entry.png`为1487×1058，SHA-256`56ec4f7826c618f4a005ee9b6952a63ca6e81506c024f496daba9e8a6abec2a4`；未编辑原图，LOGIN原图哈希亦未变。按image-to-code/design-qa流程使用同视口、同默认状态将原图与实际组件截图在同次输入中联合比较，既有字体和颜色优先，不采纳通用模板的海军蓝／新字体方案。

实际预览加载生产AppointmentChooser、SessionProvider和SessionController，OIDC及精确自身context HTTP外部边界使用合成数据；存储为独立Map，不操作用户浏览器真实线索。预览不连接身份服务，不修改生产main、配置、路由或业务数据，不代表真实登录领卡。

| 最终截图（本地执行目录下task94-choice-captures） | CSS视口 | 实际像素 | 观察 |
|---|---|---|---|
| choice-desktop-final.png | 1487×1058 | 1487×1058 | 与冻结稿联合比较；无横向或纵向溢出 |
| choice-desktop-1440-final.png | 1440×900 | 1425×891 | 主确认可见，帮助／页脚可纵向滚动，无横向溢出 |
| choice-tablet-768-final.png | 768×1024 | 753×1004 | 两栏、无合法代办说明，选中名称补充可读 |
| choice-mobile-long-final.png | 360×800 | 345×767 | 单栏，任职全文两行显示，无横向溢出 |
| choice-mobile-bottom-final.png | 360×800 | 345×767 | 键盘焦点可见，End后页脚底771.88小于800 |

实际文件像素由System.Drawing读取。除原尺寸默认态外，垂直滚动条导致截图归一化，不能把上述像素当作CSS视口，未拉伸截图或据错误密度改样式。手机DOM内容宽345／scrollWidth345，scrollHeight1207；1440内容宽1425／scrollWidth1425，scrollHeight1053；平板内容宽753／scrollWidth753，scrollHeight1099。窄屏纵向滚动是内容适配，不是永久遮挡。

五项核对：①中文系统字栈、44px右标题、24px选择值及主按钮层级一致；栅格稿与本机字宽有P3细差，不换字体。②44/56分栏及108px品牌占位沿用LOGIN，表单x736.22／宽679.72、选择框top328.99／高64、确认top751.78／高68贴合源稿x735／宽680、top329及752。③暖白／薄荷／石墨／翡翠绿、6px圆角及弱边界保留。④用户要求的Logo／律所名称占位不替换为虚构品牌；信息图标采用Phosphor，select/radio采用原生控件，因此箭头／圆点与栅格稿有P3差异。⑤姓名和任职来自当前context，样例只在验收夹具；更换当前同一任职禁用是行为约束，不伪造可操作外观。没有导航、目录、权限编辑或业务提交入口。

发现及修复：初版默认态scrollHeight1075多17px，引入滚动条且主按钮约下移21px；仅收紧CHOICE垂直间距后为1058且关键坐标贴合。手机长select截断而不能核对全文，修复为同表单可换行全文（桌面短名不重复）。风险出现／取消时焦点丢失，修复后实际焦点分别为“取消切换”／“确认本次身份”，Enter可继续。后两项均有真实断言RED→GREEN。

浏览器交互抽检：多本人不默认第一项，确立本人后才启用办理方式；合法代办即使仅一个候选也必须显式选择，未选禁确认；原代办身份可重新选回且保留线索。跨scope最终确认先显示“只删除本地线索，不撤销原操作”，取消保留提示，明确放弃合成线索后才返回已确认。无任职／无代办都有说明与禁用；退出清除当前身份显示、不声称全局注销完成。单选label高50px，动作按钮至少44px；手机确认焦点轮廓可见。最终浏览器error/warn读取为`[]`，临时视口已恢复，tab2保留本地预览。

上述浏览器检查只证明选择组件及同页状态。存储失败、候选过期、卸载／控制器替换／logout后迟到响应与不发业务或回执请求由真实controller集成测试证明，不能靠预览提示当作HTTP验收。完整恢复页、生产main/config装配、全Task9.4独立评审及真实Keycloak/API/SPA整链仍是后续门禁。

CHOICE-01 visual/interaction result: passed (bounded component preview only)

CHOICE独立评审补充：初评无Critical／Important，1项Minor为重新选择办理方式或代办候选后旧成功提示残留。Root以真实预览先确认本人再选未选定的代办复现；`95b9673`三个草稿处理处清message后同路径status为空。新增3例先RED，最终相关44项及typecheck通过；无CSS/布局变更，默认截图仍有效。预览自身回调提示也明确改为“验收历史”，不冒充当前组件状态；该夹具不入生产。最终控制台仍为空，视口保持已恢复。

## Task9.4 RECOVERY-01增量QA — 2026-09-09

本节对应用户本轮选择的第2稿，仅恢复页面及既有会话／回执接点。视觉源为`docs/design/session-access/frozen/RECOVERY-01-operation-check.png`（1487×1058，SHA-256 `175ad846b8cb6acb8fe34029418abb52b38276227bd7ba8256709387137db910`）。实际截图在`.superpowers/sdd/2026-09-08-task9-real-user-access-plan/task94-recovery-captures/`，忽略预览加载真实RecoveryPage、SessionProvider、SessionController和HTTP适配层；外部OIDC、context、Receipt与current响应为合成夹具，不连接身份服务，不使用真实线索或业务数据。

首轮对比：将原图及`desktop-01.png`在同次输入中联合展示；CSS视口1487×1058，但DOM scrollHeight1060，多2px引入滚动条、内容宽1472并使截图归一化，不能把密度差当字体尺寸问题。P2发现为默认态不必要滚动条及主按钮偏下（notice top475.72/h100、primary top603.72；源约470/h96及594/h68），已交原实施者小范围调整，禁止隐藏overflow绕过。

五项初核：既有中文系统字体栈和44px标题、24px操作及身份层级保留；左44%品牌及右分区顺序符合；暖白、薄荷、石墨、翡翠绿tokens及6px圆角保留；仅用户要求的Logo／律所名称占位与既有Phosphor信息图标，无额外图片资产；默认静态中文匹配，动态身份取context，未知不说失败。全幅主要文字清晰可辨，无需裁剪／缩放局部图来改变密度。字体栅格与系统字宽细差为P3，不替换批准字体。

初轮交互：未知查询按钮执行时禁用，GET404保留未知且没有重放动作；放弃必须二次确认，取消后焦点回到“放弃本地线索”，确认只清合成本地线索。成功回执后current503显示独立“原操作结果已确认，当前责任暂时无法读取”，重试只有CURRENT GET，成功后“继续”；401清除姓名、任职和操作名，保留重新登录／管理员说明。手机长身份全文换行、按钮高56px／次操作44px、End可读页脚，无横向溢出。

预览夹具校正：初版错误按复数receipts判断路径，并遗漏零卡chatComposer禁用字段，导致合成响应被真实校验拒绝；已按冻结单数`/receipt`及准确零卡envelope修正并重新走通上述成功／只读重试。此为忽略夹具缺陷，不改业务合同或放宽验证。

最终复拍：notice高度收为96px、operation底距收6px后，`desktop-final.png`与原图在同次输入再次联合比较。CSS／实际像素均1487×1058，scrollHeight1058，无默认滚动条；内容x736.22、宽684.18，notice top469.72／高96，primary top593.72／高68，与源约735／685、470／96、594／68吻合。初轮P2关闭，无剩余P0/P1/P2。字号／字重在冻结中文系统栈下保持层级，栅格原稿细微字宽及信息图标笔画差异记P3，不另换视觉体系。

| 最终证据 | CSS视口 | 实际截图像素 | DOM |
|---|---|---|---|
| desktop-final.png | 1487×1058 | 1487×1058 | 宽1487，scrollHeight1058 |
| desktop-1440-final.png | 1440×900 | 1425×891 | 内容／scrollWidth1425，scrollHeight1050 |
| tablet-768-final.png | 768×1024 | 753×1004 | 内容／scrollWidth753，scrollHeight1028 |
| mobile-long-final.png | 360×800 | 345×767 | 内容／scrollWidth345，scrollHeight1340，长名完整换行 |
| mobile-bottom-final.png | 360×800 | 345×767 | 放弃操作键盘焦点可见，页脚底778.82在800内 |

实际文件像素由System.Drawing读取。小视口垂直滚动条触发截图归一化，未裁切或拉伸；一次紧邻视口切换的陈旧平板帧已用稳定视口重新采集，不据其改变布局。手机／平板的自然纵向滚动不遮住永久控件。最终console error/warn为`[]`。

新增边界抽检：过期合成标记禁止查询，可在同页“只删除本地线索，不撤销原操作”二次确认后放弃，只有SELF GET、无Receipt或业务写；继续文案明确原操作仍需核对。测试另覆盖删除实际生效后存储仍抛错的失败关闭、不同Actor迟到响应及可见性切换期间解除单飞锁；测试证据见阶段记录，不以截图替代。

- [x] 同状态同视口原图／实现联合比较，并关闭首轮P2。
- [x] 字体、布局、颜色、占位／图标和中文内容五项核对。
- [x] 四尺寸、长名称、焦点、确认／取消及安全状态抽检。
- [x] 只读回执／重试请求、console与实际截图尺寸核对。

此结论仅适用于恢复组件及合成边界预览；生产main/config装配、完整Task9.4独立评审及9.6真实身份整链仍非本视觉结论。

独立评审补充：`5a8a9fe`仅修改hook及测试，修复已确认回执后的current GET被隐藏中止时无法手动重读。真实RED后最终相关42项／typecheck通过；同一评审者复审1/1关闭、无新增阻塞项。组件/CSS未变，上述视觉截图仍对应当前视觉字节；新生命周期边界由DOM/HTTP回归验证，不冒充浏览器协议链。默认本地预览保留tab2、临时视口已恢复。

final result: passed
