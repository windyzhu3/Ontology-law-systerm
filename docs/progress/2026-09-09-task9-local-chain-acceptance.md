# Task9.6 本地整链验收进度

## 当前执行：人工已签认关闭，继续Task9自动验收

用户最新确认手工验证完成，U01～U03按用户签认关闭，归档信息待补充，不要求重做。当前继续Task9.6剩余自动验收；Task9.1～9.5既有实现保持，完整Task9尚未完成。重复确认卡的历史候选缺口、最终同构建总评审与参考容量等剩余门仍分别记账；不得把本轮只读子场景扩大为R1总完成。

### 最新检查点（下方逐轮记录为历史证据）

**2026-09-12 当前：人工签认已关闭，Task9.6q等待态只读刷新验收已闭合。** U01～U03登记USER_CONFIRMED_CLOSED，不重做人工操作；实际执行时间、账号别名和构建信息待补充归档。此前延期／NOT_EXECUTED与q未实施均为历史。Task9.1～9.5既有实现保持，当前推进的是Task9.6前后端真实整链验收，不是扩展MVP业务范围。

| 本轮实际通过 | 证据与边界 |
|---|---|
| 真实请求链 | 当前责任GET共8次：初始1＋自动6＋手动1；其中7次304，真实token续期POST成功1次 |
| 状态与缓存 | 同Actor与任职、等待数1／无当前与后续卡、同语义envelope保持；六次后暂停，续期及手动刷新不重置额度；随后至少60秒无新增CURRENT请求 |
| 数据保全 | 53张非审计表、590条原审计行、84张IdP静态表、发布材料／进程／原journal／checkpoint不变；准确新增contact SELF读取审计1条，业务写入0 |
| 关闭范围 | W06等待态手动刷新／实际304／语义保持；W08自动六次额度／自然续期与手动刷新不重置的具名子集。不是非空卡披露审计、dirty／focus／迟到GET／后台／回执三次／卸载清理或W09通过 |

**源码与独立评审：** `38bbc19`新增独立只读入口并复用缓存校验；受影响业务工具215项只跑一次。锁定Playwright不提供304正文，修正仅新flow后30项／strict通过。评审发现浏览器启动失败缺安全报告，`dd3bc43`纳入现有guard／报告生命周期，行为RED后31项／strict通过、复审关闭。真实首次遇到等待区双span严格匹配，`1b9de81`只精确选择计数span；锁定Chromium离线合成DOM复现RED 1失败／2通过，最终34项／strict通过、独立复审关闭。未改产品、界面风格、API、DDL、权限、部署或依赖；原全套215未重复，继承颜色环境警告已如实记录。

**真实执行与失败保留：** 首轮基线`ddbdf6/25218 → 1db62e`退出0。首轮`64eb11/12077 → 5843b7`退出1，停于INITIAL_UI，SELF200／CURRENT200各1、0业务写入，未进入自动刷新；失败报告SHA为`bf7acb189debaeaa08a7ceda3bbdd3ef55515004ad4d9e8f25158e415512fd51`。`bd1e7a/73228 → ce95ed`独立核对原589条审计等保全通过，仅新增1条SELF；原报告／基线／退出记录全部保留，不改成PASS。

修复后明确使用独立02记录：基线`ec5c72/68396 → d92a62`退出0；真实`1f551d/77591 → 1c20ea`退出0；最终`aa18ff/65381 → e1b12d`闭包退出0，得到上表计数与保全结果。受保护`task96q-closure-02.json`绑定独占02基线、实际进程退出及唯一新报告SHA；原失败报告固定SHA仍核对。Root工具也经独立评审；CURRENT时间为请求开始、SELF／REFRESH为响应观察，不混排成虚构的全局时序。应用仍为build `421ca57`，验收工具提交不是新应用制品。

**下一步及完整Task9剩余门：** W09须到`2026-09-14 10:00 +08:00`观察实际Worker将原等待责任恢复为OPEN并通过页面刷新核对revision，不改到期时间；其余会话／故障／工作台状态、撤权代办与管理生命周期剩余具名自动项仍待验，重复确认历史候选准备仍需单独授权，最后进行同最终构建总评审。人工操作不再阻挡；容量参考环境另属R1后续门。附件／通知R2、语音后置、ADM-05～07排除范围保持。

**2026-09-12 续接：人工计划已交付，下一只读验收范围已核对，尚未实施或新增通过。** [人工验证计划](../acceptance/2026-09-12-task9-human-validation-plan.md)覆盖预约／受控准备、U01开户资格、U02本人保存提交、U03暂停恢复退出；用户本人执行，仍保持延期和NOT_EXECUTED。专用UAT身份／组织／任职／授权须开始前确认，不收养此前仅建号测试账号，不改当前自动验收身份。

下一候选Task9.6q只覆盖现有WAITING卡的六次自动刷新额度、暂停提示、自然过期后手动刷新触发真实token续期、续期不重置额度及真实304的具名子项。只读审查确认现有管理员持续会话入口不覆盖工作台，而已完成等待入口包含写入，不能重跑替代；需新增独立只读入口并复用现有保护与缓存合同，不能打开写入journal或暴露私有写入夹具。预计约6分钟：初始读取→六次30秒自动刷新→静默跨过真实token有效期→一次手动刷新与自然续期→至少60秒无新增自动刷新。全200则304为NOT_TRIGGERED，不伪造响应；未知／变化失败关闭。须在真实到期前有至少10分钟余量，否则留给W09，不改时钟。只关闭观察到的W06/W08子集，不声称dirty、迟到GET、焦点／后台、回执三次、卸载清理、W09或整组通过。新增入口的具体方案待确认；本次不运行旧测试或新业务写入。

**2026-09-12 本轮完成：Task9.6p 等待生成与页面提示已真实闭合。** 新独立场景复用原账号／组织／任职／准确首联Grant，通过真实API准备一条合成MANUAL线索，再经页面完成分配和NOT_CONNECTED／EMAIL首联。5／5命令确认、1阶段通过、无pending；2张Task DONE＋1张CONTACT Task WAITING/revision1、2份CONFIRMED Draft、1份CONTACT_RETRY_V1 WaitReceipt及5条Event／DELIVERED Outbox准确对应。页面核实无当前卡／无后续摘要、等待数量1、禁用且无目标的输入区、准确等待标题和服务端今日摘要。只关闭T9-W02／03／04具名等待子项及W09准备，不关闭完整W组。

**到期与剩余门：** 数据库确认恢复时间为`2026-09-14 10:00:00 +08:00`，原始SLA期限10:30；没有改时钟、SQL改日期或直接插Task。实际Worker恢复为OPEN及刷新后的revision仍待真实到期观察。等待期间可继续会话／故障／刷新、撤权代办和管理生命周期剩余自动项。重复确认历史候选前置仍待单独授权；U01～03延期，容量环境待提供。Task9.1～9.5保留既有实现；Task9.6及完整Task9未完成，附件／通知R2和语音后置不变。

**源码与验证：** 实施`0a1c8ba`限8个E2E文件，27项定向及一次184项受影响业务回归通过、strict通过，独立规格／质量Approved。首次真实入口`4e84d9/79157 → d8dea1`退出1；只读`64d9a8/85845 → 674ae5`及`9cdb25`确认新journal未创建、零业务命令，原因是验收入口误用run专用UUIDv4规则校验合法UUIDv7 Grant资源ID。补丁`5670ba6`仅2个E2E文件，行为RED后29项定向GREEN／strict通过，独立复审关闭问题；run／报告规则、原资源ID及授权不变，不重复184项。`926f05/40123 → 1c1f06`真实前驱加载通过。没有产品代码、界面风格、DTO、表结构或部署变更。

**真实证据与保全：** 写入前`f97d14/76736 → 0cca86`退出0，保留旧15条／3阶段及原材料，另存独占基线。首次失败01日志保留；复核后仅允许新02日志，`a5bbb8/88800 → 1cd93b`真实准备场景退出0，没有重放六卡链。最终`6f5a12/2643 → 24142b`数据库闭包退出0：原54表、537条原审计、84张IdP静态表、原六卡SHA／材料保持；41条新增审计均分类。受保护`task96p-wait-checkpoint.json`保存原任务、WaitReceipt摘要和真实到期时间，供后续只读恢复核对。本机脚本的checkpoint预检及event／receipt、draft／task关联补强已独立复核；不以修改检查脚本代替改变业务事实。

**2026-09-12 本轮完成：六卡子集前后端真实整链闭合。** 9.6o提交`d77ee271badc60688e168fe97c53485d0d37d925`仅修改7个E2E文件，不改业务、DTO、前端风格或部署。行为RED后38项定向GREEN，最终受影响业务157项只运行一次、严格类型检查退出0，独立规格／质量Approved，无Critical／Important。Root和评审未重复整套离线测试，原身份33及前后端基线复用，未重放原成功命令。

| 当前交付 | 本轮已核实的结果 | 尚不能等同于 |
|---|---|---|
| 六种责任卡 | 补齐、路由处置、来源停用请求确认、分配、首联结果、主管复核，均经过真实登录、页面保存／主提交及数据库核对 | 全部七卡；实际停用来源 |
| 原运行记录 | 3阶段、15／15命令CONFIRMED、无pending；原11条及恢复命令原字段保持 | 新开run重跑或覆盖历史失败 |
| 业务闭包 | 6 Task DONE、6 Draft CONFIRMED、2 Lead、3 Decision、1 Assignment、1 ContactResult、14 Event及DELIVERED Outbox | 等待／重开／代办／全分支完成 |
| 数据保全 | 原54表、260原审计、84原IdP表及发布材料保持；277条新增审计准确分类 | 容量或人工验收通过 |

**实际执行证据：** Root前置保全`62ef93/60994 → 8f5282`退出0。初次启动`6411ac`在浏览器前因父进程缺本地验收标记失败；`d72752`确认未创建恢复副本／05日志，未发业务请求。补齐进程级既定标记后，`65d0f9/88958 → 2ab1e9`真实第三场景`T9-W01-assign-contact-review`退出0，日志`task96k-live-six-cards-05.log`独占保留。原首联草稿只GET原成功Receipt及当前Draft后走原complete；随后页面首联提交、复核草稿／提交成功，没有重发原PUT或换键。原04及更早失败日志、九命令重启检查点、十二命令恢复前副本均保留。

**闭包与脚本修正：** 首次最终闭包在ALL_NEW_AUDITS分类处失败`5de259/2844 → 136454`，其前的Receipt/Fact/Task/Draft/Event/Outbox及原表核对已通过。只读分组`8b6242`与源码`02416b/3422ca`确认遗漏的两种合法管理员读取：GET_IDENTITY_ADMIN_OPTIONS／LIST_IDENTITY_PROVIDER_USERS；补齐后`905757/18015 → 0de8d7`定位至Actor分类第125行。定向`2b2156`只发现原delegate账号一条GET_SESSION_CONTEXT本人读取；仅允许该准确账号／本人Subject／SELF_IDENTITY／无命令路径，不增加代办权限、不计为代办业务验收。最终`be636d/53442 → 830d97`退出0，277条新增审计均通过Actor、已知Subject、schema、摘要和具名行为核对；数据及业务命令未为修脚本而改动。

最终journal SHA256：`841a4d275bf97bdb832bbb138f70efbc310dbbe10f532d6c4dbb435380d52333`。应用仍为build421ca57／release6411135a52094b6ba16a80df80b025a1／gate12；工具提交不是应用制品版本。

**下一执行单元与范围：** 先准备独立合成线索的NOT_CONNECTED→WAITING分支，核对等待数量、到期窗口、保存／提交和后继，再按真实到期时间验实际Worker恢复。冻结CONTACT_RETRY_V1规定首次未接通下一工作日当地10:00、第二次下一工作日15:00；不得改时钟、SQL改到期时间或直接插Task加速。等待期间推进不依赖到期的会话故障／刷新恢复、撤权代办及管理生命周期具名剩余场景；不再跑已闭合六卡链。新场景必须另立受控记录并绑定本次闭包，不能追加或改写这15条完成记录。下一等待场景尚未创建或执行，本段不是通过证据。

**整体仍未完成的门：** 重复确认卡历史候选前置（原具名授权缺口保留）、等待恢复、会话／故障／并发与工作台状态剩余项、撤权代办、管理生命周期、最终同构建总评审；U01～U03按用户要求延期，容量参考环境待提供。附件／通知R2、语音后置、ADM-05～07及其他R2业务均不进入本轮。

以下“当前执行／待确认／PENDING”等段落为本轮之前的历史快照，以以上完成结论为准。

**2026-09-12 当前执行：** 用户已批准下述最小比较修正及原回执受控恢复，并要求减少重复测试。9.6o仅修验收工具：保留原命令键、旧检查点及原十一条成功命令，通过精确命令／原始 journal 摘要绑定开放唯一首联草稿 PENDING 的恢复；恢复只查询原回执和当前草稿，不重发保存。正在完成定向测试与独立评审，实际恢复和后续首联／主管复核尚未执行。此前“待确认”是旧停点，不再重复请求相同批准；不复跑未受影响的身份、前端、后端基线。

**保存后确认停点已定位（源码＋离线生产序列化对照）：** `r1-business-setup.ts:471` 使用原始 `JSON.stringify` 比较草稿值，错误地要求字段顺序相同。真实 `candidate` 的赋值顺序以 `leadAssignmentId` 开始，而生产生成 CONTACT DTO 将判别字段 `resultCode` 首先序列化；两者内容一致，生产前端 `sameValues` 返回 true，脚本字符串比较返回 false。这是可确定复现的验收脚本缺陷，不需要改变业务 DTO 或前端设计。实际失败日志已脱敏，未直接保留异常行，故不声称取得了该次 live 栈；但此合法响应在第471行必然被拒绝，发生在 journal.complete 之前。独立只读诊断详见本机 SDD `task-contact-draft-diagnosis.md`。

**待确认的下一步：** 最小修正草稿内容比较并补真实消费者 RED/GREEN、独立评审；再以明确原命令／回执／草稿核对的受控恢复处理该 PENDING，不重发命令、不换键、不改旧九命令或重启检查点。该恢复不在9.6n“任何 PENDING 拒绝重开”的既有许可内，尚未实施，需单独批准后再续验首联结果与主管复核。

**2026-09-12 真实续验结果：分配已成功，首联草稿已落库但验收确认未完成。** 9.6n 提交 `3343524` 的 51 项定向／117 项业务离线／33 项身份测试及严格类型、默认离线发现通过；控制者复跑 117 项退出 0，独立规格／质量 Approved。当前运行核验仍为 release641／build421／gate12／WorkerREADY。受保护衔接凭据已独占保存，原九条命令与两阶段检查点未改写。

实际只续第三场景的 `task96k-live-six-cards-04.log` 保留退出 1（`b4de34/23664 → 1f0833`）：分配草稿和分配提交均 CONFIRMED，首联草稿为 PENDING；首联结果提交／主管复核未执行。只读数据库核对 `a916da` 退出 0：该原 PENDING 命令已存在唯一成功回执及 DRAFT/revision0 草稿，任务、Actor 和候选载荷摘要准确对应。不能据此直接改写 journal 或重新发送保存请求。当前是 **11 条 journal CONFIRMED＋1 条已有成功数据库回执但尚未确认的 PENDING**，不是 12 条验收完成，也不是完整三卡通过。

`871508/1530 → 6fc6fc` 原数据保全检查退出 0：精确新增事实 ID 排除后原 54 表、260 条原审计及原 84 张 IdP 静态表保留，原九命令／两阶段、发布和材料未变，新增测试用户保留。ContactResult／Party／Wait／Opportunity 无新增；全部新增审计尚未完成分类。按已批准重启方案，存在 PENDING 时拒绝重新打开写入；先诊断保存后的验收确认原因，再单独明确只读回执核对与受控恢复资格，不删除标记、不换命令键、不重放。

以下“9.6n实施中”及“实际凭据尚未生成”等均为执行历史，以本段为准。

**2026-09-12 继续执行：9.6m 已关闭，9.6n 按用户批准实施中。** ASSIGN 后继卡版本验收修正提交 `f08158d`，仅两处 E2E 文件；行为 RED、3 项定向 GREEN、66 项完整业务离线、严格类型检查均通过，控制者复跑 66 项退出 0，独立规格／质量评审 Approved，无 Critical／Important。这修正的是验收工具对 Lead revision +1 与新 Assignment revision 0 的区分，不是新增业务功能。

用户已确认新增 Keycloak 账号仅用于建号测试，并批准最小同构建重启衔接。精确排除这一个新增账号及其关联行后，原 84 张身份静态表全部还原基线；只读复验 `95d51e` 退出 0，原九命令 journal、发布与材料保持，新增账号保留且不新增业务授权。原九命令／两阶段不可重放；9.6n 仅新增独立旧检查点与当前进程绑定，原 identity／阶段报告保留，校验不符拒绝后续写入。实际衔接凭据尚未生成，真实验收仍为 9／15 命令，分配／首联／复核三卡未完成。人工验收继续延期，Task9.6／R1 不晋级。

以下“本轮收尾”及其他旧停点均为历史记录；以上段落为当前执行状态。

**2026-09-12 本轮收尾：Task9.6l 缓存合同修正的源码／离线／独立评审门已完成。** 提交范围 `db7fe38..d77b553`，只改三个E2E文件，未改产品设计、页面、接口、表结构、授权或部署。工作台200／304与管理200/no-store分开校验，复用真实前端解析器，隔离测试自身读取，补齐请求代次、身份与异步写入保护。最终63项业务离线、33项原身份测试、strict类型及默认离线发现通过；控制者复跑最终提交63项退出0。第三轮独立复审 `task-9.6l-rereview-3.md` 规格／质量Approved，已跟踪Important全部关闭；保留原有颜色环境警告和被准确限定的超时保证。

**当前停点不是缓存补丁未完成，而是剩余真实验收的前置条件未关闭。** 本轮0条新业务命令；原9条CONFIRMED、2阶段，分配／首联／复核三卡未完成。下一项源码修复为ASSIGN后继Lead版本断言；真实续验还需处理同构建重启后的原journal证据衔接，并确认用户新增IdP账号的来源。未收到账号变更说明前不恢复、删除或收养该变化。人工U01～U03仍延期，完整Task9.6与R1不晋级。

以下2026-09-12“续接”和旧缓存停点描述保留为本轮开始时的诊断历史，以本段收尾为最新状态。

**2026-09-12 续接：** 9.6l 已开始离线缓存合同修正，承接五轮上限后的独立诊断，不重置9.6k。新只读核对 `ac28b0/64217 → 0e7781` 已通过九命令／原前缀／准确新增事实排除后的54表与260原审计检查，在 MATERIALS 综合门失败；进一步 `888961` 分离确认部署release与全部原材料不变，84张IdP静态表中4张变化：`credential`、`user_entity`、`user_required_action`、`user_role_mapping`。不能再把下表的84表不变历史值视为本次当前状态。用户账号操作确认中，不回滚或收养变化，真实写入暂停；离线修正继续。

此前按用户请求启动服务的原进程注册备份有且仅有一份与原阶段 API 摘要对应，三进程可执行程序及参数相同，但新 API 身份已变化。既有 journal 将进程身份纳入 environmentDigest，需明确证据衔接后才能续验；不改写旧摘要、不用新 run 重做九条命令。另发现原测试对 ASSIGN 后继卡错误要求 Lead revision 不变，实际冻结业务实现是 Lead revision+1 后创建 CONTACT 卡，待独立定向修复；不改变产品行为。

| 层次 | 当前状态 |
| --- | --- |
| 业务功能实施 | 保留Task9.1～9.5既有实现；本轮没有新增MVP／R2业务或改变冻结页面。 |
| 验收工具 | 缓存合同误用已由9.6l修正，最终`d77b553`的63项业务离线回归及独立复审通过；ASSIGN后继版本断言仍待修，真实运行未新增通过。 |
| 本轮真实业务验收 | 9／15条命令CONFIRMED、2份阶段报告。补齐／路由处置／来源请求确认三卡子场景通过；首联人员授权测试通过，第二条MANUAL线索已接入。分配草稿尚未派发；分配／首联／复核三卡未完成，续验进程退出1。 |
| 现有环境与数据 | 原release641／build421／gate12及材料保持；准确新增ID排除后54表既有行、260条原审计、9个Receipt／Actor、前7条journal前缀保留，无pending。IdP四表变化，用户数8→9，来源待确认；服务重启后的API进程身份不同。新增审计尚未全部分类，不声称九命令完整闭包。 |

**当前停点：验收脚本错误套用缓存合同，不是已证实的业务分配失败。** 原run `9848f4ee-5612-49df-9e10-a8c40c09bd3d`续验`5d91b4/25096 → e087e1`退出1，保留`task96k-live-six-cards-03.log`。只读探针`dd95ee/68337 → fca35e`及`701cb0/76812 → 9d375e`均定位于缓存工作台会话刷新，业务journal字节未变、0业务写入。第二次探针观察GET为200但没有`no-store`；认证观察值受失败清空缓存及探针正则影响，不用其判定认证失败。冻结展示合同第20行和`CurrentWorkCardDisclosureService`明确工作台使用`private, no-cache`、`Vary: Authorization`并支持200／304；管理／SELF才要求`no-store`。不得改产品响应头、关闭条件缓存或降低Actor核验来迎合测试。

九命令保留核对`a46221/50300 → 496bd0`退出0，工具`output/task96k-partial9-preservation.py`验证阶段报告哈希、原journal前缀、准确Receipt／Actor／FactRef、新Grant原contact／founder／ROOT和任职期、MANUAL Lead及主管ASSIGN Task、全体既有行与原材料；不替代后续三卡及全部新增读取审计闭包。

**五轮上限处置：** 此测试缺陷真实且影响后续验收，9.6k保持未完成；不忽略、不直接追加第6轮。独立诊断`task-9.6k-postcap-diagnosis.md`已返回并完整读取（`5e07b5`），确认端点合同误用及夹具错误；原第5轮兼容性结论在此点被新证据更正。最小后续方案为按端点验证缓存合同、补真实合同形状的200／304消费测试；304必须证明当前同Actor缓存与原／请求／响应ETag准确关联，不能只把304加入成功状态列表。该修正尚未实施，先审定处置。保留原run与9条命令，只续未完成步骤。业务设计、部署、表结构及人工／容量完成门不变。

### 历史执行记录（以下“当前／最新／停点”均指各次记录时刻）

最新运行`9c8fd2/81318 → 12dbf9`保留失败日志，第一阶段报告SHA为`9706d949504abd22dbf12b5466e1af1b4013fb7c87cc9b609c4e61ebe0c1493d`。`99c007`逐命令证明准确回执／Actor／FactRef；`e97372`核对1条Lead、3张DONE Task、3份CONFIRMED Draft、2条Decision、7条Event和7条已投递Outbox及原Owner／后继因果；`fe240f/39617 → f0652e`核对原行和53条新增审计（含后续只读诊断）。受保护`task96k-phase1-root-closure.json`已独占保存（`76a3d5`，SHA `b2fd50096555b022d40783a49e4e754cf77a9c9b4008fa53ce258b289142f422`），明确保留父进程退出1及六卡未通过，不以子场景绿色覆盖失败。

旧Bearer原因已用真实只读对照确认（`2fb56d/83814 → 59142f`退出0）：原缓存令牌自然到期后读取401，使用现有SPA“刷新”得到200，随后缓存的当前令牌读取200；业务journal字节不变，0业务写入，无手工token刷新、令牌／DOM注入。修复仅让测试脚本在复用闲置会话前经已有页面刷新取得当前有效请求上下文；失败即阻止后续读取／写入，不回退旧缓存。续验必须保留原run和前7条命令，只执行尚未完成的具名阶段，不重建线索或授权。

**最新具体停点：真实测试运行器兼容性，非业务写入失败。** 第二轮`6fb51d3`已通过独立复审（`e95ed1`，原问题全部关闭）；新工具39项由Root复跑`481ddd`退出0。运行核验`9a8086`及54表／260审计／84IdP／材料完全保留核对`998fee`均退出0。随后首次真实运行`516f5e`在3.2秒内退出1、三场景均失败；受保护检查`863126`确认没有业务journal及pending标记。完整环境加载隔离检查`38ff3a/87035 → 7a73ff`退出0。无浏览器最小复现`ab5cb3`准确报“工作进程找不到项目”：主进程按argv动态注册真实project，worker重载时无该argv。稳定项目＋默认不匹配真实测试的对照`bcff60`通过（显式1项执行、默认仅离线1项）。第三轮仅修E2E配置与实际工作进程回归，不动业务设计或降低安全门；默认仍不执行真实场景。原失败日志保留，不重建身份、不换本轮编号。六卡真实整链仍0／6。

Task9.6k六卡测试驱动已进入实施：先AUTO缺联系方式→补齐→零Owner路由→停止受理请求→ACK，再通过ADM-04仅给原contact任职新增SALES_CONTACT_OWNER，最后MANUAL接入→分配→首联SUSPECT_INVALID→主管CONFIRM_INVALID。两次真实API接入、六卡真实页面保存／确认和一次管理授权均独立记录；不重新建档，不新增capture页面，不实际停用来源，不创建R2责任。源码／离线／独立评审通过后才执行真实写入。

**实现与验收分开计数：** Task9.1～9.5保留既有实现及阶段验收；当前9.6k交付的是已有六卡的自动验收工具，不是从零实现六卡前后端。截至本检查点，本轮真实业务写入0／15、六卡真实整链通过0／6；七卡总门仍未通过。页面单测通过、API分层测试通过和本地服务已部署，均不等于真实页面提交后的业务事实已经核验。

**9.6k源码评审检查点：** 实现提交`35fa1e9`仅10个E2E文件；14项新工具测试、原33项、strict类型／默认离线列表／diff检查通过，Root对提交后14项复跑`2b6c9d`退出0。独立评审提出5项Important问题：登录请求白名单绕过、续验卡片／草稿绑定不足、跨Owner记录错误、原Grant准确ID／归属及有效期核对不足、误用公开ROOT投影中不存在的revision字段。目前为第1轮修复，尚未复审通过；不能据离线绿色启动真实写入。最新运行核验`45f0aa/79437 → 77403f`及写入前保留核对`28d25d/20111 → 3d03f9`均退出0：原release641／gate12、54表／260审计、84张IdP静态表及材料与本轮基线完全相同。

ROOT数值revision1由Root精确只读数据库检查`c5806a`证明；原7项业务授权的原ID、原任职、权限码、ROOT范围、原授权人及ACTIVE状态由`aa91d9`证明。页面仍按冻结公开字段和不透明ETag检查，不新增接口字段。复审已澄清：同Actor续验必须绑定原Task／草稿／Receipt及预期输入；跨Actor后继的真实Lead／Owner因果由最终数据库闭包证明，不跨Actor比较不同用途的哈希，也不增加特权测试接口。结果未知且不能确认原后继时停止，不收养新卡、不重放。

原授权回执补核`13b00f`退出0：只读原7个命令，逐个关联Slot／Receipt／Audit，核对CREATE_AUTHORITY_GRANT成功、准确原资源／revision0／原founder Actor及无代办；按冻结公开引用合同重新计算的FactRef与原报告一致。此为原身份前驱证据，不计入本轮15次新业务写入或六卡通过数量。

**第1轮复审已返回，进入第2轮最小修复：** `b5efc0e`仅修改4个E2E文件。实现者报告最终新工具34项、原工具33项、严格类型及默认离线列表检查退出0；Root在准确提交上复跑新工具34项（`b40df5`）退出0。独立复审`4231a4`确认原5项中的4项已处理，后继因果字段确实来自冻结DTO；剩余原问题是恢复时未将准确草稿ID与原Actor回执FactRef绑定。另发现修复引入的3项授权脚本问题：跨进程第二阶段依赖内存缓存、开始时间取整到未来一分钟、把等价时间字符串误判为不同。第二轮只修这4项并补反例，不改业务合同／序列化／界面。授权取原任职区间内已生效的整分钟；不存在合法时间则在派发前停止，不扩期或增加调度。真实业务写入仍0／15，不能标记六卡通过。

本轮已完成：当前运行核验`ff75cd/85530 → 877ce8`退出0，仍为原421ca57／release641／gate12、Worker三个循环READY；`31dddf/75364 → 175741`使用已审阅的只读保留工具，在新受保护文件`task96k-before.json`独占保存54表／260条当前审计／84张IdP静态表基线，未覆盖旧证据。测试工具33项`6516be`、完整前端462项`05fc87/47660 → 4a4b2c`、拓扑`700a2f`及基线`b12da0/58490 → b90c47`均退出0。基线仍准确报告原7个非阻断R2就绪缺口，不据局部绿色晋级R1／R2。用户中途“继续”打断的一次测试启动未留下可确认结果，未算入通过；以上462项是随后独立启动并取得最终退出码的一轮。

| 后续交付 | 状态与顺序 |
| --- | --- |
| 六卡真实联通及逐命令数据库闭包 | Task9.6k实施中，尚未实际派发业务写入。先完成独立测试驱动与评审，再做真实页面操作；这不是新增六类业务功能。 |
| 重复确认卡 | 缺合法历史候选，保留待具名数据方案；不删除该验收项。 |
| 等待恢复、撤权／代办、会话／管理生命周期、错误恢复 | 原Task9.6自动项，六卡驱动后继续复用，不以人工延期为阻断。 |
| 完整BranchID报告、严格CI及容量工具 | 原Task10后续自动交付；现有foundation workflow不是九层完整R1门。 |
| 人工U01～U03／完整参考容量实测 | 前者按用户要求暂缓，后者缺参考环境；均不记PASS。 |

补充回归：类型检查与OpenAPI生成一致性`a50618`、独立输出目录构建`e21db4`均退出0，未覆盖当前运行SPA。数据库生成检查、58项schema测试（4.380秒）和21份迁移／24个PL/pgSQL函数解析`c84660`退出0。PostgreSQL harness在Linux/Python3.12按当前未改的提交源码及真实历史测试对象执行121项（7.220秒），`9f56e5/78483 → 91cd1e`退出0。后端完整`verify -Pit`的`79dabf/14614 → 960882`退出0，耗时29分38秒：本轮日志明确记录125项单元＋761项集成测试（886项），失败／错误／跳过均0。不能把目录中遗留的旧XML报告累加成本轮数量。独立jOOQ drift与baseline单测尚待最终退出结果。这些是分层回归，不是Task10九层严格顺序CI、两次空库运行或容量证据。

上述harness首次Windows/Python3.14运行退出1（121项、24失败／57错误），保留日志。已复现目录扫描`st_nlink=0`而同一文件直接`stat=1`；还存在符号链接权限和路径编码断言差异，未放宽安全校验。第一次Linux快照对照剩7个错误均缺历史Git对象；补齐准确对象后121项通过。完整历史bundle曾因部分克隆远端取对象连接失败而未生成，最终只导出测试需要的6个既有Git对象，不复制Git凭据、运行材料或业务库；两次失败记录均保留，不声明Windows工具链已经修复。

上述回归后续结果：独立jOOQ `baa82f/64687 → c15086`退出0（1项测试、52.063秒），`a157c6`确认后端／接口／生成源码无差异。Linux基线单测`c45ff2/71615 → 90c4f8`退出0（提交940a843的未改基线源码，303项、748.908秒）。Windows本机同组基线单测已出现符号链接权限和平台路径夹具错误；在Linux完整通过后，精确核对本轮自有测试进程身份并停止重复运行（`dd9270`，包装进程最终`e237d7`退出-1），状态为STOPPED_INCOMPLETE，日志保留。不标记Windows通过，不放宽安全检查，也未停止任何业务进程。

## 历史检查点：七卡前置已核对，重复确认卡缺少合法历史候选

2026-09-10继续原Task9.6，未新增业务功能。现有测试工具33项离线回归退出0（`508997`）；当前运行核验`51dcdb/66263 → e0a786`退出0，仍为release `6411135a52094b6ba16a80df80b025a1`、应用build `421ca57aed3d2f364fea2af64b12b5c9d6226c7d`、gate12，Worker数据库／mTLS／三个循环READY。本轮没有构建、部署、重启、业务写入或Git推送。

具名只读核对`f20c99/54840 → eba376`退出0：Party、Lead、Assignment、Task、Draft均为0，AuthorityGrant为14；原7阶段16命令及其材料摘要、84张IdP静态表、245条原审计逐行保持。当前审计总数260，仅作为现场读数，不归因为本轮操作、不声称全库零变化；业务54表相对保留基线仅发布状态和审计签名不同。

| 验收前置 | 当前结论与处理 |
| --- | --- |
| 当前制品与身份建档 | 旧身份测试PIN仍指向6c6c6d9／gate11。后续业务验收须新建当前制品记录并只读引用原身份闭包，不能改旧journal或重放16条建档命令。 |
| 重复确认卡 | 冻结合同要求已有RESOLVED Lead关联ACTIVE Party，现场两者均为空；相同联系方式的重复capture不足以生成该卡。历史合成数据方案尚未批准，不直接插入Party／历史Lead／Task。 |
| 其余六类卡 | 可按原capture、自动零候选、补齐、分配、首联、复核、来源请求确认路径准备，但仍未执行。先保留首联／代办无SALES_CONTACT_OWNER状态，以覆盖零候选路由。 |
| 接入与来源确认边界 | SPA没有capture表单，测试准备可用真实登录后的受权POST /api/v1/leads；七卡保存／提交仍须由页面完成。ACK只确认收到来源请求，不改变来源状态。均不新增页面／命令／权限。 |

依据：[重复候选规则](../contracts/r1/R1-TASK-COMPLETION-MATRIX.md#duplicate-candidate-registry)、`JooqDuplicateCandidateReader`与`JooqLeadRepository.capture`；独立只读合同核对得到相同结论。这是测试数据前置缺口，不是改变重复判定规则的理由。建议先继续其余六卡，重复确认卡另行确认仅本地、单独留痕的最小历史合成数据方案；不建设当事人管理／导入平台，不伪造历史业务回执或Task。待用户确认该后续安排，本轮停在新增数据或范围变更之前。

整体仍为Task9.1～9.5已有阶段验收、9.6j已完成；七卡／管理生命周期／撤权代办等待恢复／最终同构建回归及用户本人U01～U03未完成。附件与通知仍属R2，语音后置，参考容量环境仍待提供。

## 已完成：Task9.6j与真实自然刷新故障关闭

用户已批准的最小修正由`e6f3a7e31ad7bbfe96ba1062f859ebf2d2611a82`完成：独立`release-verify-current-runtime`复用原校验器，并严格绑定当前包及三个运行进程；原Java、原始引导CLI、Worker验证器、SQL、SPA和依赖均未改。新增测试分别经历缺入口／缺CLI／既有受控来源发布包不兼容的RED，最终定向7项、完整本地部署工具90项（65.607秒）退出0。首个89项全量早于最后修正，不当作最终证据。Root精确最终源码定向7项复跑（`0e1885`）退出0，独立规格／质量复审Approved，Critical／Important／Minor均0。

| 本轮真实验收门 | 结果与证据 |
| --- | --- |
| 当前发布运行核验 | `fda198/34803 → 5054dc`退出0，`VERIFIED_CURRENT_RUNTIME`；准确release／应用build／gate12，Worker数据库／mTLS／三个循环READY、listeners0。不是`VERIFIED_ORIGINAL`，旧严格核验失败记录保留。 |
| 管理员登录协议 | `9df09f/7526 → f9f581`退出0，Code兑换200，nonce／issuer／audience／auth_time检查通过，SELF200 READY、管理入口true／工作台false，测试会话退出204。 |
| 未映射账号 | 同组命令退出0；Token兑换200但SELF401，测试会话退出204，未自动建档或授权。 |
| 真实自然刷新 | 原探针`e115ad/40315 → 8ed937`退出0。真实登录并确认身份，自然等待250秒；初始nonce存在，refresh_token HTTP200且新ID Token无nonce，随后组织GET200、页面正常加载，无重新登录、保活或业务写入，blockedRequests0。 |
| 数据／环境保留 | `97d6e6`退出0：54表中仅相对部署前基线的发布状态和正常读取审计变化；245条原审计逐行不变，仅新增SELF披露2条、Identity披露2条。84IdP静态表、原材料、原7阶段16命令保持。`69a117`进一步确认相同release／gate／源码输入／三个进程登记／原journal及报告哈希。 |

现场应用仍为`421ca57aed3d2f364fea2af64b12b5c9d6226c7d`，release`6411135a52094b6ba16a80df80b025a1`，gate12。本轮仅运维核验源码更新，未重新构建、部署、重启、执行IdP管理或业务数据写入。运维源码`e6f3a7e`与应用制品源码分别留证。浏览器正常认证会改变IdP动态会话，不把84静态表声明扩成整个IdP零变化。

受保护新探针报告`task96e-session-control-00d7d193-68ba-494b-b0c6-a67402bba650.json`，SHA256 `13f2a69acd05c473ccca4813a323d4778ce9f398e5bc479a76348d48e00ba3f4`；沿用已评审保留脚本首次生成的`task96i-after-ui.json`，SHA256 `1a7918c5dbf26fbed7ff5fee2c2af5263d7f5034631a6590882d66308b35eb03`。文件前缀沿用原工具名，实际属于本次新验证；正文ACTIONS_VERIFIED连同真实退出0及`69a117`最终绑定才作为证据，不覆盖任何旧失败或原创建清单。

**整体位置：** 当前完成的是基础登录／身份与发布联通的验收补洞，不是新增MVP业务。Task9.1～9.5保持已有阶段验收；9.6i／9.6j及本次具名自然刷新子场景完成，整个Task9.6仍未完成。下一步按原计划继续真实七卡链，再做管理生命周期、撤权／代办／等待恢复及最终同构建回归；用户本人U01～U03仍未执行。容量环境等待用户后续提供，附件／通知仍在R2，语音后置。一次250秒刷新成功不等于全部会话寿命／连续多轮轮换验收，也不倒推历史两次600秒自动化超时的唯一根因。

下方均为保留的历史执行记录，早先“待确认／未部署／刷新阻断”以本页顶部最新具名结果为准。

本轮前置只读核对`38b71d/6181 → 56963a`退出0：当前release`6411135a52094b6ba16a80df80b025a1`／应用源码`421ca57`／gate12与上一轮部署一致，已打包源码输入未变，三个登记进程存在且登记SHA256为`662184c9978128a89e5587dd8f8a8f955231d8fba5b9f6102c329a069882994c`。54表按原内容及已知发布gate分别核对，245原审计、84IdP静态表、原材料与7/16清单不变。首次诊断只在输出摘要时引用了不存在的辅助函数而失败，修正诊断调用后完整重验通过，未改数据。发布测试单文件基线首次缺PYTHONPATH导致8个import错误，正确设置后27项退出0；不是产品修复的RED。

## 历史停点：9.6i完成后，发布后引导核验阻断自然刷新验收

受控构建绑定`421ca57aed3d2f364fea2af64b12b5c9d6226c7d`：锁定JDK25.0.4.1+1运行`mvnw.cmd -B -f backend/pom.xml -DskipTests package`实际退出0（`1a2734/67597 → e113d2`，Maven BUILD SUCCESS）；锁定Node24.20.0／npm11.9.0和四项固定本地OIDC值运行`npm-cli.js run build`退出0（`43f35e`，含tsc）。`-DskipTests`不是新后端测试证据；既有OpenAPI生成器mutualTLS诊断保留，不声称构建日志无告警。首次短SHA capture被40位校验拒绝，改用完整SHA后退出0，未发生部分发布。

新包`6411135a52094b6ba16a80df80b025a1`暂存／摘要核对通过；准确停止原API／SPA／Worker后，原受控CAS从gate11推进至12并启动三进程，Keycloak和数据库未停止。`release-status`为COMPLETE／new／schemaMatches；登录及四个管理页面直接TLS入口均200（`9ecf7d`）。新包Worker健康（`c88ab2/60412 → 3ef94b`）退出0，database及mTLS READY、requiredLoops3、listeners0。制品Jar SHA256为`d496603eaeda6e7893f002e4c1ade7c317369b743eec21fd39210cde43bec586`，manifest为`051010dfc50c3259cb52ae5af09f5cb1013639e0a91fdba8773893ceba2d61eb`。

**仍未通过的发布后门：** `bootstrap-verify-current-release`（`c3b462/89376 → 6424be`）内部CLI退出2、外层退出1，原始失败日志及本轮专用副本均保留。源码`JooqIdentityBootstrapService.verify`明确要求原ROOT名称和revision0，而当前经原合法改名闭包验证的ROOT为revision1；该核验按既有合同要求原事实版本严格不变，与将其当作正常演进环境的重复发布门不一致。这不是Worker运行核验再次失败。当前批准范围保留首次授予／原始核验严格语义，不能顺带修改Java引导合同或绕过门禁。因此后续protocol-check／unmapped及250秒真实自然刷新尚未执行，不将制品切换成功等同于整次发布验收完成。

部署后保留检查`f0c0c7`退出0：54表中只有`platform_meta.deployment_state`变化，245条原审计逐行摘要不变、没有新增披露审计；84张IdP静态表、受保护材料及原7阶段／16命令不变。`90f1e0`再次确认精确新release／gate12／build及原journal SHA256，after-deploy报告SHA256为`2188009443b94acf87689a0079ba5ebbcd9724668912e8902689f757ca596b84`。没有业务或IdP管理写入、未重放原创建；自然刷新和after-ui报告未执行。

**下一最小合同澄清待用户确认：** 保留`verifyOriginal`“原始事实仍原样”的严格语义；将正常后续发布的“当前运行状态核验”与之区分，仅复用已审查的同ROOT合法改名证据链，并保持原引导Slot／Receipt／Audit、其他原事实、权限、证书、schema及制品门严格。先明确发布流程使用哪一门，再作最小实现／回归／独立评审，随后恢复协议及自然刷新验收。不回退组织、不重新引导或补授权、不扩展业务审计合同。本次不自动实施这一独立合同变化，新包服务保留运行，不擅自回滚。

### 本轮源码及发布前门证据

`9ce2a5a`／`d44524b`／`02ce7a0`完成运行核验分离及两轮评审修复。完整无变化／拒绝改名证据可与成功版本链共存，但损坏证据不能被忽略；首次授予与其他身份、三项固定授权、证书规则保持严格。最终独立复审Approved，无未决finding。定向32项通过，Root对精确`02ce7a0`重跑全部本地部署工具83项（`a4c27c`，66.650秒，退出0）。

原环境实际`worker-health`（`4c44f7/75301 → b3d05a`，退出0）已恢复READY：database及mTLS READY、requiredLoops3、listeners0。这是新只读验证器对原环境真实SQL／进程／证书核验，不是重新授予或新部署；原ROOT保持revision1。审计未保存改名正文，不宣称重建原payload或If-Match。受保护基线为54业务表、245条原审计及84张IdP静态表，原7阶段／16命令保留。

上述源码及发布前健康证据不替代发布后验收；实际新停点见本页顶部。七卡、生命周期、撤权／代办／等待恢复及本人U01～U03仍待验收，不扩大到R2。以下为按时间保留的历史记录，早先“待确认／正在修复”的停点以本页顶部最新结论为准。

## 最新执行：2026-09-10 Task9.6i Worker运行校验修正

用户已确认9.6h停点的最小修正设计，正在实施“首次授权核验／现有Worker运行资格核验”分离。只允许凭准确ROOT改名Slot／Receipt／Audit闭包接纳名称及版本演进；首次授予、固定三项授权、其他身份字段、状态、范围、证书和失败关闭保持严格。基线Worker单元24/24通过。原环境只读核对有且仅有一条原ROOT成功改名证据，结果revision1／原授权锚点revision0／原founder Actor一致；审计未保存改名正文，不宣称重建原payload或If-Match摘要。不新增审计合同或业务权限。源码、独立评审、发布前健康门和部署后真实刷新复验分别留证，尚未完成的门不得提前标为通过。

## 最新执行：2026-09-10 Task9.6h 已批准刷新修复

用户已确认下文的最小修复设计，源码`451d0ed`仅修改OIDC adapter及测试；历史“待确认”停点已被本次批准取代。真实锁定SDK刷新回归按RED→GREEN完成（首次2/9失败、身份绑定补齐3/19失败，最终19/19通过），完整前端27文件／462项、离线安全33项及类型检查均退出0，独立规格／质量评审Approved，无Critical／Important／Minor未决项。Root新鲜定向19/19（`4ba025`）和类型检查（`7dccaf`）退出0；首次Root npm透传产生参数警告，改用锁定vitest直接入口后输出无警告，不是产品失败。源码门完成；产品部署、真实自然刷新和完整9.6验收尚未通过。

发布前发现独立的既有Worker校验合同冲突：运行校验要求ROOT与首次授予清单整行相同，而此前获批改名已使名称变化、revision从0变为1。只读检查`e907f2`确认只有这两处差异；`worker-health`实际退出1，不是本轮新代码或发布造成。停止在新构建／部署之前，旧服务未被停止；不能据此推断Worker进程已经停止，也不能宣称当前健康门通过。

本轮不修改Worker授权／清单、不回退组织数据、不绕过启动门禁。下一独立最小设计待确认：区分“首次授予原始快照验证”和“既有Worker运行资格验证”，仅接纳有原命令／回执／审计依据的合法ROOT改名，其他身份、状态、范围、三项固定授权及证书校验保持严格；确认后另补反例、独立评审与原环境只读核验，再发布本轮nonce修复。不新增产品功能。

保留核对`ad8f35`退出0：54张业务表（含审计、gate）完整表集合和全表签名未变，84张IdP静态表、凭据／密钥／realm材料及原7阶段／16命令报告摘要未变。IdP动态会话表不在静态核对声明内。辅助诊断脚本的表集合／审计证据边界问题经窄复查关闭；本轮实际仅使用before-ui严格审计签名模式，未执行after-ui数量模式。

## 最新结论：2026-09-10持续会话复验

**9.6仍未完成：测试驱动异步边界修复已完成源码验证及独立复审；真实令牌刷新后错误退出的产品缺陷尚待修复。** 未改冻结页面、业务权限、Keycloak配置或部署制品；原7阶段／16条身份创建证据保留。

测试驱动提交`9e4ab39`仅涉及11个E2E文件：Python环境检查改为异步等待，原保护检查不缓存／不省略；写入门在异步工作前抢占，journal保留排他、未决隔离与最终原子发布。独立评审提出原journal identity与当前环境绑定及失败分类问题，窄修复`31d34c6`增加5项RED→GREEN反例并逐项关闭；复审规格／质量通过，无未决finding。Root最终复跑离线33/33（`2d135f`退出0）、strict类型检查（`e5c896`无诊断／退出0）、diff-check（`54814d`退出0）。新独立只读持续会话入口尚未执行，不计PASS；已知产品刷新缺陷不能通过修改探针绕过。没有重跑已完成7阶段，没有产品部署或推送。

控制者使用真实登录、无保活、无同步边界阻塞的独立浏览器，等待250秒后点击组织管理。有效探针`581399/84973 → 4f3e79`退出1：初始ID Token有nonce；实际refresh_token请求HTTP200，新ID Token无nonce，access token尚未到期，issuer及auth_time基本形状检查有效；随后出现登录按钮、管理导航消失，组织API请求未发出，业务写入及阻断请求均0。凭据和token只入内存，证据只记录布尔值／固定状态；受保护报告`task96e-session-control-17f2e6b6-fb15-4f82-9426-a0bc780652a9.json`。

后置独立核对`f75d39`退出0：原journal完整SHA256仍为`44c95f59853fa552d2d7ba933dcb80a4877464fe26dab7c34202d3e1fb0ad9a1`，无pending隔离；gate11、API／SPA／Worker进程、release及业务制品与执行前相同。有效控制失败报告SHA256为`90efd4cd95db27a3081e4c9dc6013ff01c63153b1794c773022a3a73619d4095`。浏览器读取可产生正常披露审计及IdP会话，不将“无业务写入”误报为数据库所有表零变化。

独立源码检查确认`oidcAdapter.ts`在首次回跳及刷新后复用nonce必填条件；锁定SDK正常刷新会替换ID Token，不补回原nonce。合法无nonce刷新因而被前端清空会话并映射为SessionFailure(503)，**不是Keycloak返回503**。刷新省略nonce符合[OIDC Core §12.2](https://openid.net/specs/openid-connect-core-1_0.html#RefreshTokenResponse)。现有adapter正例立即取token，未触发实际refresh HTTP，不能覆盖此缺陷。

第一控制探针`f2ebae`已记录refresh200后读取失败但诊断不够细；第二增强探针`352d22`在按钮出现时过早检查、尚未等HTTP完成，不作为产品结论。三份失败报告全部保留。上述现场证据确认当前无nonce刷新故障，不反向认定历史I05/I06两次600秒超时的唯一根因。

**下一最小修复待确认：** 区分首次callback与同会话refresh验证；首次state/nonce/PKCE严格规则不变，刷新nonce可省略、携带则必须等于初始已验证值；原身份／认证时间绑定、10秒上限、失败清理、内存凭据和会话寿命保持。不以延长会话、重新登录或改变Keycloak配置绕过。确认后补真实SDK刷新回归、独立评审与新制品同环境复验，再继续七卡／生命周期／撤权代办等待及用户本人U01～U03。

## 已完成身份阶段：2026-09-10分段续跑

**9.6e人员建档与最小资格链的7项真实浏览器子场景已分段通过，16条创建命令完成数据库闭包核验；完整9.6／Task9仍未完成。** 本轮是已有前后端的真实联通验证，不是新增MVP业务；未改变冻结界面、产品代码或部署配置。

- 通过真实管理页面新增四个HUMAN绑定、一个`LOCAL_ACCEPTANCE`组织、四个任职、七项DIRECT业务授权。受理／主管能进入有效空工作台；首联／代办仍无业务权限，未授予`SALES_CONTACT_OWNER`。
- 同run `74a496f6-494e-417d-9abd-69a85c94f165`、业务build `6c6c6d90105b6d647fd213afed0c30dd9ff3a594`、environmentDigest `50862c6a02b2a76038d9bc4f2223e7a0dbdd0d4677f85dcd7418af47025c7ab9`。原L01-entry／L03-unmapped加本次I02-exact-directory-binding、L04-qualification-stages、I05-appointment-no-implicit-grant、I06-minimum-business-grants、I13-dynamic-entry，共7个子场景；不等同于矩阵同名ID的全部内容。新鲜离线安全harness18/18退出0，API／SPA／Worker进程身份未变。
- 单一`REPEATABLE READ READ ONLY`快照核对16条原commandId：Slot／Receipt／命令审计各16，CREATE均201，resultFact、原Actor、管理授权证据、组织anchor、subject HMAC、公开factRef一致，无额外命令审计或孤儿联结。12条非Principal命令可独立重建payload摘要；4条Principal原不透明selector未保留，不能事后重算其payload，也不冒称重建原HTTP正文摘要。
- 剔除准确`tenant+UUID`新增事实后，原四类身份表及Slot／Receipt全表摘要与本轮前基线一致；原bootstrap审计整行一致，其他47表及关键材料一致。当前全表为Principal6／组织3／任职6／Grant14，执行前为2／2／2／7，另已有3条Slot／Receipt含既有组织创建／改名。旧口头“组织1”不是当前全表基线。ROOT在本轮前已为revision1；旧Worker快照revision0只用于原UUID定位，当前版本先经完整基线摘要证明未变再核对本轮引用，未改数据库迎合旧快照。未扩称原80条审计全部逐行复核。
- 闭包脚本的报告发布／同快照／tenant维度／额外审计门经独立复审，实际核验退出0，并再次验证正式completion与正文哈希。受保护证据`task96e-closure-f42e66b0-75bb-4d95-8957-0afb65260781-completion.json`绑定SHA256 `3451a7fe4cab69ef86f6aa906bdec7488ab4e103c316e0c2283e3d4b55718ae6`。journal为7个哈希绑定阶段、16条CONFIRMED；单独ACTIONS_VERIFIED正文不是完成凭证。

**未关闭：长流程自动化的两次超时。** I05在组织命令发出前超时；I06在15条命令已确认后超时，最后一项授权未发出。失败报告和原key全部保留；无未决写入、换key重发或删除记录，随后按同run以新浏览器会话只续接未执行部分，I05/I06/I13分别退出0。分段成功不证明原超时已修复或持续会话稳定性通过。

只读诊断中，新会话组织入口及准确ROOT候选正常；静态GET对照为正常9ms、现有snapshot＋三次protect阻塞Node12595ms时浏览器10008ms超时。它证明同步测试边界可能干扰浏览器请求，但不是两次原失败的确定根因；未放宽生产超时或关闭安全门。

**下一步顺序：** 先收紧测试驱动的同步边界／路由调度与安全诊断，再继续既定七卡、管理生命周期、撤权／代办／等待恢复；最后由用户本人执行U01～U03（目前全部`NOT_EXECUTED`）。Task10／R1发布不晋级，容量环境继续等待用户提供。

## 历史过程与分阶段记录

日期：2026-09-09建立，2026-09-10更新。状态：**进行中，Task9未完成**。范围依据：[Task9扩展计划](../superpowers/plans/2026-09-08-task9-real-user-access-plan.md)与[验收矩阵](../acceptance/2026-09-08-task9-real-user-access-acceptance.md)。

**本次结论：9.6g管理入口修复已完成源码、独立评审、本地部署和两个真实入口子场景验证。** 原办理身份确认页现可通过受当前本人管理资格约束的“进入身份管理”按钮进入管理模式，并再次确认身份。沿用冻结样式及内存token，不新增侧栏或更改认证／权限规则。仍属于Task9真实用户接入和联通验收，不是新增MVP业务；完整Task9尚未完成。下文“待确认／待重验”为保留的历史过程，不是当前入口阻断。

原失败run `a59d863c-716a-4399-9a5f-95a0e5c12a83`再次核对为零命令、零已完成阶段、无未决提交；只读数据库仍为HUMAN1/SERVICE1/组织1/任职2/Grant7/Lead0/Task0。已将单个空命令清单在同一受保护目录原样留档，前后SHA256一致，失败报告原位保留且未变。没有删除证据、收养业务事实或把失败改为通过；新构建重验使用新run，不冒充旧环境续跑。

9.6g源码提交`4db2509`：受资格约束的站内管理入口、重新确认本人任职、原恢复优先规则，以及真实E2E驱动的站内点击／精确未映射401断言。入口缺失行为RED与GREEN、450项前端回归、18项离线harness、前端及独立strict E2E类型检查退出0；独立评审通过。测试制品绑定`1a07f85`经独立复审，仅同步新包五值和独立正例，保留初次正例仍指向旧包的17/18失败；最终18/18通过，校验逻辑未放宽。

**9.6g最新部署结果：** 独立规格／质量评审通过，无Critical或Important；一项非阻断建议为失效后detached节点点击测试未必执行React旧handler，不能夸大其证明范围。实际构建来源`6c6c6d9`（含已评审`4db2509`及后续进度文档），Jar／SPA实际构建退出0，新包`2db735dbaddc435fb585483f5393f40e`、gate11；Jar `b51bd7dba4648000d1a09ea6ffef4ba3bb4eab17bc922a05935ef5e8619623f7`、manifest `c5f374ee58e4c21b8c2b726cc5e1fe8e35f7fbe2128a2d5900bb8d910e950844`。原引导核验通过，54表核验前后不变；三进程准确使用新包，Worker三循环READY且零监听，管理员SELF200、未映射401、退出204和五个TLS HTML入口通过。原材料、来源配置及SERVICE绑定保持；旧包保留。

首次新包启动返回一次无详细信息的RuntimeError；后续只读核验确认原三进程已停、端口空闲、秘密完整、无启动pending标记。未改代码或清标记，仅带异常位置记录重现正常启动后成功。首次失败保留为未复现／未定位观察，不声称根因已修复。这与下述已定位的测试客户端CA问题分别记录，不混为一个原因。

**真实入口结果：** 新run `74a496f6-494e-417d-9abd-69a85c94f165`、environmentDigest `50862c6a02b2a76038d9bc4f2223e7a0dbdd0d4677f85dcd7418af47025c7ab9`。首试因Node APIRequestContext缺少本地CA信任而失败：Chromium的SELF200和站内UI正常，但Node读取接口前证书链错误。对同一无凭据GET作单变量验证，未加载CA时失败，仅为测试进程设置既有CA的`NODE_EXTRA_CA_CERTS`后TLS验证通过且HTTP200；没有关闭TLS校验、改全局信任或替换证书。实际CA SHA256为`dcb6d289c9b9f025dbaaebd3fa6a0af9482e845a44911d6df6489cf83724b44c`，执行前先走原protect，前置说明已写入`e2e/README.md`（`858b864`）。

随后显式续跑同一run及同一运行环境，`T9-L01-entry`与`T9-L03-unmapped`均通过，实际退出0。L01记录两次SELF200及原组织／主体GET200；L03记录原未映射账号和四个尚未建档专用账号的五次SELF401。Root核对两条durable阶段与各自报告哈希一致、无pending、commands0；首次FAILED报告和旧run全部保留，三进程身份未变。数据库仍HUMAN1/SERVICE1/组织1/任职2/Grant7/Lead0/Task0，未新增目标业务建档。仅这两个子场景通过，不等于7场景、完整矩阵或人工UAT通过。

## 当前处于什么阶段

Task9.5的管理页面、十四项管理写入、工作台状态提示已完成源码及受控界面验证。本轮不是新增MVP业务，也不是重新设计页面，而是把已有前后端装配到同一本地真实运行环境，验证真实登录、受控建档、责任卡和Worker恢复的完整链条。

| 环节 | 当前状态 | 不能据此宣称的结果 |
| --- | --- | --- |
| 已有前端源码 | 最新450项／27文件通过，退出0；含9.6g的新SPA已部署 | 回归和部署不代替真实业务操作整链 |
| 本地登录环境 | Keycloak TLS、准确issuer及原管理员／未映射账号协议检查通过 | 不代表四个新业务账号已取得资格或七卡已实测 |
| 原bootstrap保护 | 当前运行制品核验`VERIFIED_ORIGINAL`；停Worker核验时54表及原文件摘要不变 | 原闭包核验不认证后来新增事实是否合法 |
| 9.6b制品切换 | 已完成：独立复审、真实新旧制品回退与恢复、TLS入口、登录及原闭包核验通过 | 不是七卡／人工整链验收完成 |
| 9.6c Worker | 已完成：锁序修复与独立复审通过；真实三项授权、同Jar启动、三循环就绪和停启复验通过 | 没有真实等待Task恢复证据，不代表W09或七卡已通过 |
| 9.6d 自动来源配置 | 已完成：固定AUTO来源发布、未使用时真实回退及再发布通过，原人工来源和SERVICE绑定保持 | 尚无真实路由卡／等待／停用确认卡事实，不代表七卡已通过 |
| 9.6e真实账号与受控管理链 | 7/7身份子场景分段通过；16条真实CREATE及数据库闭包核验通过 | 两次长流程超时仍待定位／修正；七卡与持续会话稳定性不在本次通过范围内 |
| 9.6f管理成功响应合同修复 | 两文件修复56c096e通过独立评审；HTTP11项、后端单元125项、前端适配器108项通过；新制品已本地切换并复验 | 此修复已完成，但不代表管理入口或Task9整链通过 |
| 9.6g管理站内入口 | 已完成：子代理实现、独立规格／质量评审、同构建部署、真实站内进入和未映射拒绝通过 | 仅关闭入口缺口，不代表业务建档／七卡／UAT完成 |
| 七卡、撤权与故障恢复 | 待同构建真实验收 | 既有分层测试不替代全部整链用例 |
| U01～U03人工验收 | 用户已指定由本人执行；未执行，[模板](../acceptance/task9-real-user-uat-template.md)已更新 | 指定人员、自动化和合成账号均不算使用者签认 |

## 本轮已核实证据

以下按发生顺序保留初始失败及修复过程；当前结论以上表为准，不将历史“待复验”误作当前阻断。

**最新真实运行结果（2026-09-10）：** run `a59d863c-716a-4399-9a5f-95a0e5c12a83`，业务build `7967b45`，environmentDigest `7beaa746aafda18a0e94305ce8a7e553cd3e9696e7ff131129b7b49052201665`。首场景`T9-L01-entry`取得真实SELF200后超时；其他六项失败于前置条件，不是六个独立业务缺陷，也不是跳过即通过。实际退出1。原失败JSON和journal保留：commands0、completedStages0、无未决提交或completion隔离。结束后只读核对HUMAN1/SERVICE1/组织1/任职2/Grant7/Lead0/Task0，三进程身份摘要不变；没有目标业务建档，更没有UAT通过。

**当前入口阻断与待确认修复：** 测试登录后整页跳转管理地址会丢失仅存内存的token；当前非callback初始化不自动恢复登录，继而回到登录页，脚本等待的身份确认按钮不会出现。进一步核对真实界面：普通登录固定到工作台流程，管理员虽有管理资格却只有“可使用身份管理地址”的提示，没有可点击的站内管理入口；直接输入地址又触发上述重载。这不能靠测试`history/evaluate`改地址掩盖。拟在现有办理身份确认页补充受当前本人管理资格限制的站内入口，沿用冻结样式、单页切换并明确确认身份；不加侧栏、不持久化token、不变更OIDC固定callback，须先由用户确认。另一个测试驱动错误是把未映射SELF固定断言为403及内部文案，而实际冻结接口允许且本地返回401，页面显示“请重新登录以核对当前会话。”；后续修复精确断言，不泛化放行。以下回执地址发现为历史记录，已由9.6f解决。

**当前真实写入前置阻断（2026-09-10）：** `IdentityCommandRuntime.result`把管理成功响应Location设为资源地址，但冻结OpenAPI的十四个成功响应均引用原命令`ReceiptLocation`；前端`identityApi`正确要求回执地址。推导影响为后端成功后前端保留未决标记、无法按普通成功继续；尚未在本地创建目标事实复现，因此不声称已出现真实脏数据。旧HTTP测试从错误Location取Fact ID，未覆盖这一合同差异。9.6f只修后端地址构造与对应HTTP回归，前端校验／合同／页面样式不变；独立评审和更新本地同构建后再运行9.6e。

**上述阻断的最新处理（2026-09-10）：** 9.6f已完成源码修复、行为RED/GREEN、独立评审和真实本地升级。当前业务构建为`7967b45`（含修复`56c096e`），发布包`4d76799837014be8931b1122ec00d867`、gate revision10、Jar `1e5fda1e83511810d0484d38ec10946392b18b3bfb824e951c912a455d1febe4`、manifest `31ac7a32b277f9efd5743a11e1115c41de189d9ca37bf44f9d459a7773958d9f`。原引导核验通过且54表/原材料未变；三进程准确消费当前包，Worker READY；原管理员登录/SELF/退出、未映射拒绝和五个TLS页面入口通过。AUTO、MANUAL及原SERVICE绑定保留。旧包与构建记录均保留。当前待办变为9.6e测试侧新包绑定与复审，再真实受控建档；尚未新增目标业务身份、任职、授权或责任卡。既有Maven/Flyway警告保留记录，不借本修复扩大维护范围。

- 起点`c3083de`，原隔离工作树与分支不变；计划检查点`01213b1`。没有推送GitHub。
- 前端基线：2026-09-09 21:11:51开始，445项／27文件，11.93秒，退出0；Node24.20.0／npm11.9.0。
- 后端单元基线：`mvnw.cmd -B -f backend/pom.xml test`，125项，失败／错误／跳过均0，退出0，21:38:44完成，用时1分30秒，JDK25.0.4.1／Maven3.9.16。未执行package，不替换运行Jar；这不是实库IT／最终E2E总验收。原OpenAPI生成器的3.1／注解默认值／组合Schema警告保留，不声称输出无警告。
- 本地helper基线：Python4项与Node2项全部通过，退出0；后续新增测试需另记，不覆盖历史证据。
- Keycloak discovery返回200，issuer为`https://localhost:19443/realms/local-r1`，TLS正常校验。
- 本轮切换前真实SPA入口诊断：`/login`为200，四条`/admin/identity/`管理导航均为404，TLS校验开启。这是部署接线缺口的现场证据，不是管理页面源代码缺失；修复后须对同路径复验。
- 当前生产类执行原引导核验返回`VERIFIED_ORIGINAL`与空delta；54张表及原manifest、operator、密钥、配置、Jar内容摘要不变。此核验不载入测试类，也不替换旧运行Jar。
- Worker数据库登录为NOINHERIT，仅属于`law_app_worker`，无SUPERUSER／CREATEDB／CREATEROLE／REPLICATION／BYPASSRLS标志；现有SERVICE证书检查时有效，到期为2026-09-16 06:29:24 UTC。
- `scripts/verify_topology.py`退出0；`scripts/baseline/verify_baseline.py .`退出0，基线一致性PASS，仍保留原7项非致命R2发布门阻断。没有为使检查变绿而提前修改R1／R2交付状态。
- 首版独立评审发现构建输入清单缺项，`db9086d`统一纳入HTML入口、tsconfig、实际jOOQ源码及构建配置；21项定向测试通过，独立复审关闭阻断。
- 真实`snapshot-release`退出0，旧Jar／dist／配置／gate已保存。随后`stop-apps`暴露Windows进程竞态：API已成功退出，但后续`Wait-Process`查不到该PID返回1，流程未继续停止SPA。只读已退出PID及新建隐藏测试子进程均重现；`c9e85b3`使用已核对进程句柄等待，24项定向测试通过（含真实停止拒绝和20秒超时），独立复审后才继续真实操作。该故障发生时尚未构建或切换gate。
- 上述停机故障随后经独立复审并现场关闭。固定源码`b7f4a12`执行真实Maven package与SPA构建，均退出0；新制品`05475ac485914f0cb2b8fe33a11f89df`暂存／激活／启动均退出0。发布状态`COMPLETE / new / schemaMatches=true`，旧集保留。388项源码输入、3个SPA文件及Jar／manifest摘要已固定。
- 激活后五条TLS页面路径（登录和四个管理导航）全部200并返回HTML。管理员真实Code＋PKCE登录、nonce／issuer／audience检查通过，SELF为READY／管理可进入／工作台不可进入，退出204；未映射账号SELF仍为401，退出204。均退出0，不冒充专用业务资格或七卡通过。
- 新Jar原闭包核验进程退出0，末行返回`VERIFIED_ORIGINAL`，但本地wrapper将前置jOOQ日志一并当作JSON，导致自身返回错误。`ef2f7e6`补齐严格末行结果校验，定向7＋2项测试通过，待独立复审和现场复验；wrapper正式通过前不关闭此项。
- 上述结果读取修复已独立复审且现场退出0。随后真实回退至旧业务制品，原管理员登录正常；使用已审查当前生产类核验原引导，54表与原文件前后摘要不变。再固定`04bd695`执行真实构建（Jar／SPA均退出0），恢复新制品`e0310e1b63b748db8ae46de005ab0a64`，部署revision为6，状态`COMPLETE / new / schemaMatches=true`。最终五条TLS页面路径、管理员／未映射账号认证均复验通过；当前发布核验退出0，54表和原文件摘要前后不变。旧Jar仍不冒充包含新核验逻辑。
- 按批准执行本轮本地Keycloak临时管理恢复：原节点短暂停止，锁定镜像／相同数据库与TLS配置执行官方恢复命令，原节点随后恢复。通过管理REST创建`task9-local-intake`、`task9-local-supervisor`、`task9-local-contact`、`task9-local-delegate`四个合成账号；没有修改业务库HUMAN事实。
- 临时管理client删除204；原临时凭据取令牌401，已签发管理令牌也被拒绝；只读实库核对该client、服务账号及其角色映射均为0。临时恢复容器已删除，未删除原容器或数据库卷；临时secret从操作清单清除，测试密码仅留在受保护独立凭据文件。原founder／unmapped目录结果、公钥集合与目录只读权限前后相同。
- 目录直接权限仍只有`query-users`与`view-users`；令牌中的`query-groups`来自原`view-users`复合权限，已通过只读数据库关系核对，不是本轮加权。
- Worker源码`5fae852`，首版56项Python＋4项Node测试通过。独立评审发现授权事务缺少既定业务排他围栏；`b2c5ad4`补齐READ COMMITTED与业务→身份事务级排他锁，RED覆盖首次／重复两分支，9项授权及24项Worker回归退出0，独立复审关闭I1。不改生产Java、权限码或表结构。
- 真实`worker-grant`首次退出0、delta3，54表前后仅`identity.authority_grant`变化；同清单重复执行退出0、delta0。原SERVICE仍为同一身份／任职，新增的仅三项ROOT基础权限；无HUMAN业务建档。随后原引导核验退出0，54表与原材料在核验前后保持不变。
- 真实证书副本准备与独立Worker启动退出0；就绪输出为`READY / requiredLoops=3 / listeners=0 / database=READY / mtls=READY`。保持同一当前Jar与release／manifest，证书副本仅alias适配，原证书／私钥及信任材料未改。此为联合循环健康，不是具体等待Task恢复完成。
- 真实`stop-apps`退出0，准确停止API、SPA和登记Worker，保留Keycloak与数据库；停机时原闭包核验退出0、54表及原材料摘要不变。`start-apps`复用当前制品及固定资格恢复三个进程，退出0；Worker健康、管理员登录／退出、未映射账号401、五个TLS页面200及发布状态`COMPLETE / new / schemaMatches=true`均复验通过。未重建业务Jar／SPA，也未推送。
- M1保留为非阻断限制：中断启动遗留的pending标记需要准确进程核对后显式处置；不自动清除、不按进程名杀进程。正常登记进程的停启已实测；不将其冒充所有中断场景均自动恢复。
- 已准备锁定的Playwright 1.63.0／Chromium 153.0.8010.12（revision1243）浏览器缓存，安装退出0；项目测试依赖与E2E代码尚待接入，安装提示不冒充测试通过。使用该浏览器实际访问当前TLS登录页并截图退出0，Logo和律所名称占位正常；未输入账号凭据，不替代L01或人工验收。
- 9.6d源码`17a1e1d`通过17项来源定向测试、75项受影响Python及4项Node回归，退出0，独立spec／quality评审无新增阻断。真实固定配置发布→未使用时Owner只读事实检查与回退→旧包Worker就绪→重新暂存／发布均通过；未重建Jar或SPA。
- 2026-09-10最终配置包为`aabce4e3252946e4952cbcb41ff280d1`，gate revision9；manifest为`8478c05d4d783f85b7d49340e05ac667edb3ea081915e71c12efa511b5a62a25`。业务二进制来源仍为`04bd695`，Jar摘要仍为`b8af135f74cdafcb8396ee4d55e0526cec359ce65ee801ddd91f058e3bc2513a`；配置工具出处单独记录为`17a1e1d`。原`LOCAL_SYNTHETIC=MANUAL`、新增`LOCAL_SYNTHETIC_AUTO=AUTOMATIC`与原SERVICE单一来源绑定已准确核对。
- 最终API／Worker manifest一致，Worker三循环READY／零监听，原管理员登录及退出、未映射账号401、五个TLS入口200、发布`COMPLETE / new / schemaMatches=true`均复验通过。每次切换后在Worker停机时进行原闭包核验，54表与原材料在核验前后不变。已有来源事实时禁止移除配置的分支当前为合成测试证据，尚未用真实Lead实测；本轮没有创建、删除或回滚任何Lead／Task事实。

## 授权与保持不变的边界

仅原本地测试环境、专用合成账号和最小资格；不生产、不真实业务资料、不Git推送。临时Keycloak管理权限只用于本轮测试开户，完成立即移除并验证，应用目录client继续只读。原bootstrap、密钥、数据库卷和事实保留。

本轮不新增表、产品角色、业务接口、动态策略或调度平台。附件上传与通知留在R2，语音后置；原冻结前端风格与四页布局保持。容量参考环境和Task10总验收不因本地功能验证而降级或提前通过。

真实时间边界：生产`RetryPolicy`按下一工作日10:00／15:00安排前两次首联自动重试；`R1BusinessTime.nextWindow`安排路由等待恢复。真实部署验收不修改系统时钟、冻结策略或数据库到期字段。任务建立后记录实际due时间，到期再收Worker恢复证据；原使用业务时钟夹具的分层测试不替代本项。

## 下一证据检查点

1. 9.6b已关闭本地部署门；同制品验收记录保留，后续变更重验受影响部分。
2. 9.6c／9.6d本地部署门已关闭；后续在当前同制品环境进入真实管理建档与资格链。
3. 接续现有run `74a496f6-494e-417d-9abd-69a85c94f165`中尚未执行的`T9-I02`、`T9-L04`、`T9-I05`、`T9-I06`、`T9-I13`：受控建档→任职→最小授权→动态资格，再做七类责任卡与故障／代办恢复。续跑必须核对同制品／同三进程／同environmentDigest，显式设置原run的CONTINUE并加载已批准CA；不重新执行已完成阶段、不清除journal，环境漂移则停止核对。用实际证据逐项填写矩阵。
4. 环境与专用测试卡就绪后，由已指定的用户本人执行并确认U01～U03，再进行Task9整范围独立评审和完成判定。

## 已澄清的配置与授权边界（保留历史原因）

原`LOCAL_SYNTHETIC`固定为MANUAL。生产`LeadCommands.next`在此配置下产生人工分配卡；只有AUTOMATIC且无合格销售Owner才产生路由处置卡，后者才能经真实命令产生等待和来源停用确认。因此“仅暂不给销售权限就能覆盖全部七卡”的准备设想已撤回。用户随后批准固定合成AUTO来源，9.6d已发布并验证；原人工来源不改，不能直接插入Task凑齐验收。

首次Keycloak临时管理批准只限创建四个测试账号并立即移除。用户随后另行批准仅这四个合成账号的IdP禁用／恢复及管理端会话撤销；该组测试尚未执行，不能复用已撤除权限，执行完仍须移除临时管理权限并核验。业务ADM和正常SPA退出仍按已批准路径。人工验收已指定用户本人，尚未执行或签认。

**补充授权执行状态：** 用户批准的固定自动路由来源已在9.6d完成发布验证。仅四个新测试账号的禁用／恢复／管理端会话撤销尚未执行，临时管理权限结束后仍须再次移除；不涉及原管理员或密码重置。用户已回复由本人执行人工验收，实际操作及签认仍未执行。
