# Task9.5：管理前端接线与页面交付进度

更新：2026-09-09。状态：**9.5a非视觉接线与9.5b四页读取／受保护入口已验收；按已批准交互继续9.5c十四写入；整体9.5／Task9未完成。**

## 当前到底在做什么

本阶段属于已经批准的 Task9 MVP 前端实施，不是新增基础平台，也不是 R2。Task9.3 已完成受控身份管理后端，Task9.4 已完成登录／会话源码与生产入口；本地登录子集和原 bootstrap 集合核验也已有各自证据。这些完成项不代表四张管理页已打通。

| 交付部分 | 当前状态 | 完成依据／剩余门槛 |
| --- | --- | --- |
| 用户、组织、任职、直接授权后端 | 已有阶段验收 | [Task9.3证据](2026-09-09-task9-identity-admin-acceptance.md)，不是页面验收 |
| 登录与会话、当前工作台入口 | 已有源码及本地登录子集证据 | [会话阶段证据](2026-09-09-task9-session-acceptance.md)、[本地登录证据](2026-09-09-task9-local-login-verification.md) |
| 9.5a：6类读取、14条管理写入前端接线 | 已验收，仅非视觉适配器 | 实现`f2891d4`／修复`0c5b2b0`；3项Important独立复审全部关闭 |
| 9.5b：四页读取、详情、分页与受保护入口 | 源码与受控UI阶段已验收 | `b7f8524`＋补测`687252f`，351项完整／补测后33项受影响回归，独立spec和quality复审通过 |
| 9.5c：四页表单、十四写入与二次确认 | 按已批准交互开始实施 | 新增／改名沿用详情区；危险操作确认影响及原因；待全部按钮、恢复及联通证据 |
| 工作台完整状态提示 | 尚未完成本阶段验收 | 今日／当前／后续／等待、保存／提交／刷新／恢复独立状态及交互矩阵 |
| Task9.6最终整链与人工UAT | 未完成 | 同最终构建，管理路径建档到真实用户获得责任卡；真实使用者自行输入凭据 |

## 本轮范围与验证口径

当前推进9.5b读取／准入单元，起点47978c2完整前端336项／21文件通过（18:37:04，37.82秒，退出0），这是起点回归而非新增页面验收。9.5b评审通过后接9.5c十四写入，随后补工作台完整状态与9.6整链；不把只读页面当完整管理功能。用户确认“详情区编辑＋二次确认框”已写入[视觉补充](../design/identity-admin-mvp/revisions/2026-09-09-contract-alignment/README.md)及既有实施计划，不再为相同交互重复要求确认。

### 9.5b读取与准入候选证据

源码提交`b7f85240ce575e15575cf4806b839558788b59b0`。四页通过真实生产API适配器读取，只允许明确确认后的本人管理资格；游标分页、选择／重读、加载／空／错误、同Actor续期与身份失效清屏均有覆盖。组织仅展示获权已加载关系，不推断完整树。

最终定向32项、完整前端351项／22文件通过（19:27:00开始，9.16秒，退出0）；类型、隔离构建、OpenAPI检查均退出0。实际baseline／topology退出0／0，原7项非致命R2准入阻塞未改变。原七张冻结图保持，基线检查现明确区分四张具名已批准补充图；缺少原图／补充图或未知额外图仍拒绝。

[视觉与交互检查](../../design-qa.md)记录四页1487联合源图及360／768／1440核对。修复了行命中区、窄屏导航、平板多列表裁切与分页随表格横移；表格现单独横滚，分页保持可见。浏览器使用真实组件／会话控制器／适配器配受控fetch，不是实际IdP／API／数据库整链。运行中SPA的全部文件哈希与本轮开始相同，没有更新本地登录部署。

独立评审发现的必需入口验收遗漏已由测试提交`687252f`补齐，复审I1为ADDRESSED，spec与quality均批准。真实SessionApplication从有管理资格的本人切到合法代办后，管理GET为零、不披露导航或记录，并保留代办身份而非自动退回本人；最终受影响33项／2文件及类型检查通过。初次“代办＋管理资格=true”的无效夹具被既有解析合同拒绝，未放宽校验，最终使用合同有效响应；该失败不是产品缺陷RED。

9.5b现仅关闭读取／准入阶段；组织展开按钮缺少`aria-expanded`作为非阻断改进保留。所有写入入口仍明确禁用，下一步9.5c十四条按钮接线及9.5d状态收口尚未交付。实际管理开户到真实用户取卡仍待9.6，不能据本阶段宣称完整前后端整链已验收。

下文9.5a保留历史与最终验收证据；不混作9.5b／9.5c结果。

9.5a只交付非视觉的管理API适配器。使用同一个`SessionController.recovery`及现有Receipt接口，保留四字段本地恢复标记。管理只允许本人DIRECT；不能把合法代办静默转成本人管理。既有七卡与草稿请求保持兼容。

14条写入按静态命令分别验证。创建不携带虚构目标／If-Match；既有资源变更保留准确目标及原Identity ETag。结果未知不生成新key，不以失败GET证明未提交；同身份凭据轮换保留原请求。页面尚未装配，所以适配器测试即使通过，也不能标记“管理页面前后端打通”。

起点：`4ffa57c`；计划分段提交`f500c08`。起点完整前端223项／19文件通过，Node24.20.0、npm11.9.0。Root实际baseline和topology检查退出码均0，保留原7项非致命R2准入阻塞。这些是本轮起点／范围检查，不是新实现完成证据。

### 9.5a稳定候选验证（独立评审前）

首版实现提交`f2891d4b3ef0626dabda5f04225cde77f666894f`，仅6个前端源／测试文件；下列结果对应此首版候选，最终修复版本及验收见下一节：

| 检查 | 实际结果 | 边界 |
| --- | --- | --- |
| 完整前端回归 | 323项／21文件，退出0，37.72秒 | 包含原223项；不是浏览器整链 |
| 最终定向管理／共享会话回归 | 128项／4文件，退出0，3.22秒 | 4文件包含原有业务和会话测试，并非全量测试后新增文件 |
| 类型检查 | 退出0 | 精确生成DTO和判别联合 |
| 隔离构建 | 退出0，4581模块 | Vite提示外部输出目录不自动清空，未强制清理 |
| OpenAPI漂移检查 | 退出0 | 未改合同、生成类型或依赖 |
| Root实际基线／拓扑 | 退出0／0 | 原7项R2阻塞保持 |
| 运行中页面产物哈希 | 构建前后相等 | 未覆盖`apps/workbench/dist` |

最早行为RED为16项中15失败／1通过，准确捕获缺失的14请求映射与新增共享门。后续读取、校验、回执、生命周期和缓存要求经历增量RED/GREEN；部分只保留执行时输出，未逐项保存历史日志，不能声称每条新增断言都有独立的预实施RED文件。较早322项全量通过后新增了成功写响应的`no-store`校验；最终323项覆盖该修正，不能混用中间版本作最终证据。

独立评审以完整`4ffa57c→f2891d4`变更为输入，结论为需要修复，0项Critical、3项Important：异常正文／流失败的安全错误和会话失效边界、写入与恢复回执的准确终态限制、生成写入结果类型。已交回同一实现者做窄修复和定向回归，再由同一评审者复审。上述323项测试属于首版候选，不能用它宣称这些评审发现已经关闭。

两个Minor保留：读取映射测试未直接断言pathname（当前静态路径正确）；隔离构建的非自动清空提示为预期且保留说明。后者不能通过强制清目录消除。

### 修复与最终验收

修复提交`0c5b2b0b9b02301f106c1d2ea9db52c7bde420e0`。同一评审者对修复差异逐项复审，I1／I2／I3均为ADDRESSED，没有新的Critical／Important；spec与quality均批准本非视觉单元。M1／M2为非阻断后续记录，不隐藏也不扩展本轮修复。

- 每个请求独立处理响应头和解析失败，当前身份的401／403在正文读取前触发失效，迟到的旧身份响应不能失效新身份；未知结果保留恢复线索，原始正文／网络错误不作为可见文案。
- 成功写入只接受生成合同中的Identity成功回执；恢复读取要求准确200。仅两种改名允许NO_CHANGE；共享回执校验同时覆盖管理和工作台恢复路径，仍保留合法业务NO_CHANGE及Identity拒绝终态恢复。
- 写入、六读取与回执均通过运行时校验后返回准确生成类型，增加具名类型断言。

修复RED为运行时10失败／131通过及类型检查失败；修复后定向141项／4文件、受影响消费者188项／7文件、类型检查和隔离构建均退出0。Root在固定修复提交上亲自运行完整前端回归：**336项／21文件通过，退出0，37.21秒，17:56:07开始**。这是最终源码结果，区别于上节首版323项。

修复后实际baseline／topology均退出0，仍为原7项R2准入阻塞。从本轮起点到最终修复的合同、生成文件、依赖、后端和部署差异为0；再次比较运行中SPA产物，文件哈希与构建前完全相同。未推送、未激活新制品、未更改实际配置／数据。

构建产物写入本计划的隔离输出目录，不覆盖本地登录服务当前使用的`apps/workbench/dist`。本轮不推送、不更新在线Jar／配置／数据，不建立真实用户或新增权限。

## 页面视觉与合同差异：局部修订已确认

已逐张查看四张冻结高保真。高保真包含搜索、部分创建时间／身份来源／历史任职／授权人字段，而当前受控列表接口不提供这些查询或字段；组织列表是有界分页，也不保证一次返回完整组织树。

用户于2026-09-09审阅四张基于原冻结稿的局部修订图后明确回复“确认”，见[已批准修订记录](../design/identity-admin-mvp/revisions/2026-09-09-contract-alignment/README.md)。原冻结稿继续约束布局／样式；本次只批准字段、控件、分页、层级说明及合同对应操作文案，图像编辑产生的细微间距变化不成为新规范。此前三个替代布局作废，不实施。

已确认隐藏无数据依据的控件／字段，使用已有分页，只展示已加载且获权的组织层级。不新增接口、不无界拉取、不把当前一页过滤冒充整个目录搜索、不用设计示例数据代替实际响应。新增身份流程中的完整用户名精确候选查询仍保留；未关闭完整表单、错误／刷新／恢复状态和真实页面联通验收。下一步为既定ADM-01～04真实页面与现有会话／API接线；若遇未覆盖的实质性视觉选择，仍单独确认。

既定排除保持：SERVICE管理、ADM-05～07、通知／附件、语音、HR、动态权限矩阵。本阶段没有修改这些范围。
